{
  "$comment": "AUTOGENERATED by scripts/ops/cli-reference.ts — DO NOT EDIT. Edit the tool's defineCommand() block and re-run.",
  "note": "One entry per invocable leaf command. `--quiet`, `--color`, `--help` and the `--json` alias are omitted from every input_schema as noise; output is json when stdout is not a terminal; and a command with `writes: true` changes nothing until --apply (plus --yes off a terminal).",
  "count": 82,
  "tools": [
    {
      "name": "cli-conformance",
      "summary": "Check that every operator CLI is registered and on the shared harness.",
      "when": "Run this after adding a CLI, or when `npm run check:cli` fails. It answers two questions the\ngenerated reference cannot: is anything claimed that does not exist, and is anything present\nthat was never declared.",
      "parentWhen": "",
      "description": "npm run cli-conformance -- — Check that every operator CLI is registered and on the shared harness.\n\nRun this after adding a CLI, or when `npm run check:cli` fails. It answers two questions the\ngenerated reference cannot: is anything claimed that does not exist, and is anything present\nthat was never declared.\n\nReports MISSING (the manifest names a file that is gone), NOT_CONVERTED and\nNO_ENTRYPOINT_GUARD (a tier A/B tool that is not on the harness) and UNTIERED (a CLI-shaped\nfile the manifest omits). The census currently walks scripts/ops only — see the 🛑 note in\nlib/cli/census.ts before widening it.\n\nRead-only. This command changes nothing.\n\nExit codes: 0, 1, 2, 130. For this command, 1 = at least one finding.\n\nExamples:\n  npm run check:cli\n  npm run check:cli -- --tier a",
      "input_schema": {
        "type": "object",
        "properties": {
          "tier": {
            "type": "string",
            "description": "Only report findings for files in this tier",
            "enum": [
              "a",
              "b",
              "c",
              "lib"
            ]
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "cli-conformance { --tier?: \"a\"|\"b\"|\"c\"|\"lib\" }",
      "invocation": "npm run cli-conformance --",
      "file": "scripts/ops/cli-conformance.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        130
      ]
    },
    {
      "name": "cli-reference",
      "summary": "Regenerate the committed CLI documentation from the tools' own declarations.",
      "when": "Run this after changing any tool's defineCommand() block — its summary, flags, examples or\nsubcommands. The generated files are committed, and `npm test` fails when they are stale, so\nthis is the fix for that failure. Never hand-edit the generated files.",
      "parentWhen": "",
      "description": "npm run cli-reference -- — Regenerate the committed CLI documentation from the tools' own declarations.\n\nRun this after changing any tool's defineCommand() block — its summary, flags, examples or\nsubcommands. The generated files are committed, and `npm test` fails when they are stale, so\nthis is the fix for that failure. Never hand-edit the generated files.\n\nWrites <dir>/CLI_README.md (the full --help), docs/cli-reference.md (an index only) and\ndocs/cli-tools.json (the machine catalogue). Discovery is the registry in lib/cli/tiers.ts,\nnot a glob, and a file is statically checked for `export const cmd` / `defineCommand(` /\n`import.meta.url` before it is imported — importing an unconverted tool would RUN it.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nExit codes: 0, 1, 2, 130. For this command, 1 = generated files are stale (--check), or a listed tool could not be loaded.\n\nExamples:\n  npm run cli:reference\n  npm run cli:reference -- --apply\n  npm run cli:reference -- --check",
      "input_schema": {
        "type": "object",
        "properties": {
          "check": {
            "type": "boolean",
            "description": "Write nothing; exit 1 if any generated file is stale or a tool failed to load"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        }
      },
      "signature": "cli-reference { --check?: boolean }",
      "invocation": "npm run cli-reference --",
      "file": "scripts/ops/cli-reference.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        130
      ]
    },
    {
      "name": "liveone__api",
      "summary": "One authenticated request to the deployed API, as you.",
      "when": "The escape hatch for endpoints no verb covers yet — never extract the CLI token from the\nstore by hand. Prefer the purpose-built verbs where they exist (`liveone find <what>`).",
      "parentWhen": "The entry point for operating LiveOne from a terminal or an agent. Pick the domain that owns\nthe thing you want to read or change — `dashboard` for what a dashboard SHOWS — then the verb.\nRun `liveone <domain> --help` to see a domain's verbs.",
      "description": "npm run liveone -- api — One authenticated request to the deployed API, as you.\n\nThe escape hatch for endpoints no verb covers yet — never extract the CLI token from the\nstore by hand. Prefer the purpose-built verbs where they exist (`liveone find <what>`).\n\nAbout `liveone` generally: The entry point for operating LiveOne from a terminal or an agent. Pick the domain that owns\nthe thing you want to read or change — `dashboard` for what a dashboard SHOWS — then the verb.\nRun `liveone <domain> --help` to see a domain's verbs.\n\nGET runs immediately (reads are the default path); any other method is dry-run by default\nand needs --apply. The edge only admits CLI tokens on the routes listed in cliTokenRoutes\n(lib/route-matchers.ts) — elsewhere this reports the middleware 404 (protect-rewrite) and\nthat is by design, not a bug. The response body is the output, verbatim.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone api /api/cli-auth/whoami\n  liveone api /api/v4/dashboards --method=POST --body-file=dash.json --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "The request path, starting /api/ (query string allowed)"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "method": {
            "type": "string",
            "description": "HTTP method",
            "enum": [
              "GET",
              "POST",
              "PUT",
              "PATCH",
              "DELETE"
            ],
            "default": "GET"
          },
          "body": {
            "type": "string",
            "description": "Request body, inline JSON (non-GET only) (json)"
          },
          "body-file": {
            "type": "string",
            "description": "Request body, from a JSON file (non-GET only) (path)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "path"
        ]
      },
      "signature": "liveone api <path> { --base-url?: string; --method?: \"GET\"|\"POST\"|\"PUT\"|\"PATCH\"|\"DELETE\"; --body?: string; --body-file?: string }",
      "invocation": "npm run liveone -- api",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__devices__add",
      "summary": "Add one or more devices to the area, keeping the rest.",
      "when": "Adding is the safe direction: it grows the pool and cannot orphan a binding.",
      "parentWhen": "Membership is the POOL a binding may draw from — a point can only fill a role slot if its\ndevice is already a member. So this comes first, and `area role` picks within it.",
      "description": "npm run liveone -- area devices add — Add one or more devices to the area, keeping the rest.\n\nAdding is the safe direction: it grows the pool and cannot orphan a binding.\n\nAbout `devices` generally: Membership is the POOL a binding may draw from — a point can only fill a role slot if its\ndevice is already a member. So this comes first, and `area role` picks within it.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the membership (the reason says why).\n\nExamples:\n  liveone area devices add kew 10002\n  liveone area devices add kew 10002 --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "device": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Devices to add"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "area",
          "device"
        ]
      },
      "signature": "liveone area devices add <area> <device...> { --base-url?: string }",
      "invocation": "npm run liveone -- area devices add",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__devices__list",
      "summary": "The area's member devices.",
      "when": "",
      "parentWhen": "Membership is the POOL a binding may draw from — a point can only fill a role slot if its\ndevice is already a member. So this comes first, and `area role` picks within it.",
      "description": "npm run liveone -- area devices list — The area's member devices.\n\nAbout `devices` generally: Membership is the POOL a binding may draw from — a point can only fill a role slot if its\ndevice is already a member. So this comes first, and `area role` picks within it.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone area devices list kew",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "area"
        ]
      },
      "signature": "liveone area devices list <area> { --base-url?: string }",
      "invocation": "npm run liveone -- area devices list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__devices__remove",
      "summary": "Remove devices from the area — and their bindings with them.",
      "when": "🛑 Removing a member DELETES that member's bindings. This verb names them before it does,\nand refuses to proceed silently.",
      "parentWhen": "Membership is the POOL a binding may draw from — a point can only fill a role slot if its\ndevice is already a member. So this comes first, and `area role` picks within it.",
      "description": "npm run liveone -- area devices remove — Remove devices from the area — and their bindings with them.\n\n🛑 Removing a member DELETES that member's bindings. This verb names them before it does,\nand refuses to proceed silently.\n\nAbout `devices` generally: Membership is the POOL a binding may draw from — a point can only fill a role slot if its\ndevice is already a member. So this comes first, and `area role` picks within it.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the membership (the reason says why).\n\nExamples:\n  liveone area devices remove kew 10002 --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "device": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Devices to remove"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "area",
          "device"
        ]
      },
      "signature": "liveone area devices remove <area> <device...> { --base-url?: string }",
      "invocation": "npm run liveone -- area devices remove",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__devices__set",
      "summary": "Declare the exact membership — anything omitted is removed.",
      "when": "The full-replace form, matching the route. Prefer `add`/`remove` unless you genuinely mean\n'these and only these'.",
      "parentWhen": "Membership is the POOL a binding may draw from — a point can only fill a role slot if its\ndevice is already a member. So this comes first, and `area role` picks within it.",
      "description": "npm run liveone -- area devices set — Declare the exact membership — anything omitted is removed.\n\nThe full-replace form, matching the route. Prefer `add`/`remove` unless you genuinely mean\n'these and only these'.\n\nAbout `devices` generally: Membership is the POOL a binding may draw from — a point can only fill a role slot if its\ndevice is already a member. So this comes first, and `area role` picks within it.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the membership (the reason says why).\n\nExamples:\n  liveone area devices set kew 13 10002 --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "device": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The complete membership"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "area",
          "device"
        ]
      },
      "signature": "liveone area devices set <area> <device...> { --base-url?: string }",
      "invocation": "npm run liveone -- area devices set",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__flows",
      "summary": "The rolled-up source×load energy-flow matrix (the Sankey) for a period.",
      "when": "Use this to download WHERE an area's energy came from and went to over a period, with the\nattributed cost/emissions/renewable legs — the multi-day rollup of the per-day Sankey.",
      "parentWhen": "Reach for this for the SEMANTIC layer: what an area is made of and what it measured. For the\nphysical/vendor layer use `device`; for what a dashboard shows use `dashboard`.",
      "description": "npm run liveone -- area flows — The rolled-up source×load energy-flow matrix (the Sankey) for a period.\n\nUse this to download WHERE an area's energy came from and went to over a period, with the\nattributed cost/emissions/renewable legs — the multi-day rollup of the per-day Sankey.\n\nAbout `area` generally: Reach for this for the SEMANTIC layer: what an area is made of and what it measured. For the\nphysical/vendor layer use `device`; for what a dashboard shows use `dashboard`.\n\nReads the materialized per-day matrices (`flow_attr_1d` via /api/history) and folds them:\nenergy is a plain sum; each metric leg sums only the days where it is known, and carries a\nknown-kWh denominator so averages divide by known energy only. Windows are capped at 13\nmonths (the 1d serving cap). The matrix is per-AREA — never sum a multi-device area with\nits member devices.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = no attributed flow for the window (the reason says why).\n\nExamples:\n  liveone area flows daylesford --last=90d\n  liveone area flows daylesford --start=2026-01-01 --end=2026-06-30 --format=csv --out=flows.csv",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "last": {
            "type": "string",
            "description": "Relative window ending today (default: 30d) (30d)"
          },
          "start": {
            "type": "string",
            "description": "Window start (local days) (YYYY-MM-DD)"
          },
          "end": {
            "type": "string",
            "description": "Window end, inclusive (YYYY-MM-DD)"
          },
          "per-day": {
            "type": "boolean",
            "description": "Also carry the raw per-day matrices (CSV: one row per day×edge)"
          },
          "out": {
            "type": "string",
            "description": "Write the CSV/JSON to this file; stdout gets a summary (path)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json",
              "csv"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "area"
        ]
      },
      "signature": "liveone area flows <area> { --base-url?: string; --last?: string; --start?: string; --end?: string; --per-day?: boolean; --out?: string }",
      "invocation": "npm run liveone -- area flows",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__history",
      "summary": "Time series for an area, in the OpenNEM shape /api/history serves.",
      "when": "Use this to pull an area's measured series over a window. The human rendering is a\nper-series summary; the full payload goes to --out (or --format json).",
      "parentWhen": "Reach for this for the SEMANTIC layer: what an area is made of and what it measured. For the\nphysical/vendor layer use `device`; for what a dashboard shows use `dashboard`.",
      "description": "npm run liveone -- area history — Time series for an area, in the OpenNEM shape /api/history serves.\n\nUse this to pull an area's measured series over a window. The human rendering is a\nper-series summary; the full payload goes to --out (or --format json).\n\nAbout `area` generally: Reach for this for the SEMANTIC layer: what an area is made of and what it measured. For the\nphysical/vendor layer use `device`; for what a dashboard shows use `dashboard`.\n\n--start/--end are whole LOCAL days (the area's fixed day offset — the same boundaries\nthe daily aggregates roll up on). One request regardless of span; bound long sub-daily\npulls with --series. Shapes: --format json nests the full OpenNEM body under `response`;\n--out writes the RAW body; each series carries\nhistory.{firstInterval,lastInterval,interval,numIntervals,data}.\n--format csv emits WIDE rows — timestamp_local, timestamp_utc, then one column per\nseries with the unit in the header (`13/load/power.avg (W)`); nulls are empty cells.\nWith --out the CSV goes to the file and stdout gets the summary (as JSON).\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = no series matched (the window, or the --list-series subject).\n\nExamples:\n  liveone area history daylesford --list-series\n  liveone area history daylesford --last=3d --interval=30m\n  liveone area history daylesford --interval=30m --last=7d --series=\"load/*\" --format=csv --out=load.csv\n  liveone area history daylesford --interval=1d --start=2026-07-01 --end=2026-07-31 --out=july.json",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "interval": {
            "type": "string",
            "description": "Series resolution (range caps per request: 5m ≤ 31 days, 30m/1d ≤ 13 months)",
            "enum": [
              "5m",
              "30m",
              "1d"
            ],
            "default": "5m"
          },
          "last": {
            "type": "string",
            "description": "Relative window ending now, e.g. 3h, 7d (default: 1d; the server owns the grammar) (7d)"
          },
          "start": {
            "type": "string",
            "description": "Window start — whole LOCAL days (the subject's fixed day offset) (YYYY-MM-DD)"
          },
          "end": {
            "type": "string",
            "description": "Window end, inclusive (local days) (YYYY-MM-DD)"
          },
          "series": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Only series matching this glob, matched against the DEVICE-LESS path, e.g. \"load/*\", \"**/energy.delta\" (repeatable; `*` does not cross `/`) (glob)"
          },
          "out": {
            "type": "string",
            "description": "Write the raw OpenNEM body (or the CSV, under --format csv) to this file; stdout gets a summary (path)"
          },
          "list-series": {
            "type": "boolean",
            "description": "List series METADATA only — id, unit, metric type, stat suffix, declared intervals, data extents and sample count; no data arrays. The natural first call against an unfamiliar subject. Refuses time flags; --interval is ignored (the per-series `intervals` field answers it)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json",
              "csv"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "area"
        ]
      },
      "signature": "liveone area history <area> { --base-url?: string; --interval?: \"5m\"|\"30m\"|\"1d\"; --last?: string; --start?: string; --end?: string; --series?: string[]; --out …",
      "invocation": "npm run liveone -- area history",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__latest",
      "summary": "The area's current values, from the serving cache.",
      "when": "Use this for 'what is it doing NOW' — the same latest map every dashboard card reads.\nFor anything with a time axis use `history`.",
      "parentWhen": "Reach for this for the SEMANTIC layer: what an area is made of and what it measured. For the\nphysical/vendor layer use `device`; for what a dashboard shows use `dashboard`.",
      "description": "npm run liveone -- area latest — The area's current values, from the serving cache.\n\nUse this for 'what is it doing NOW' — the same latest map every dashboard card reads.\nFor anything with a time axis use `history`.\n\nAbout `area` generally: Reach for this for the SEMANTIC layer: what an area is made of and what it measured. For the\nphysical/vendor layer use `device`; for what a dashboard shows use `dashboard`.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone area latest daylesford",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "area"
        ]
      },
      "signature": "liveone area latest <area> { --base-url?: string }",
      "invocation": "npm run liveone -- area latest",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__list",
      "summary": "List the areas you can read: id, handle, name.",
      "when": "Start here when you do not yet know an area's id.",
      "parentWhen": "Reach for this for the SEMANTIC layer: what an area is made of and what it measured. For the\nphysical/vendor layer use `device`; for what a dashboard shows use `dashboard`.",
      "description": "npm run liveone -- area list — List the areas you can read: id, handle, name.\n\nStart here when you do not yet know an area's id.\n\nAbout `area` generally: Reach for this for the SEMANTIC layer: what an area is made of and what it measured. For the\nphysical/vendor layer use `device`; for what a dashboard shows use `dashboard`.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone area list\n  liveone area list --format json",
      "input_schema": {
        "type": "object",
        "properties": {
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone area list { --base-url?: string }",
      "invocation": "npm run liveone -- area list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__role__clear",
      "summary": "Empty a (role, metric) slot, or every slot of a role.",
      "when": "Give a role alone to clear all of its metrics; give both to clear one slot. The area falls\nback to union-default resolution for whatever is cleared.",
      "parentWhen": "Reach for this when an area renders a card empty, or a metric it should have is missing —\nmost often because the device is a member but nothing binds its points to a role.",
      "description": "npm run liveone -- area role clear — Empty a (role, metric) slot, or every slot of a role.\n\nGive a role alone to clear all of its metrics; give both to clear one slot. The area falls\nback to union-default resolution for whatever is cleared.\n\nAbout `role` generally: Reach for this when an area renders a card empty, or a metric it should have is missing —\nmost often because the device is a member but nothing binds its points to a role.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the change (the reason says why).\n\nExamples:\n  liveone area role clear kew grid rate --apply\n  liveone area role clear kew grid --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "role": {
            "type": "string",
            "description": "The role to clear"
          },
          "metric": {
            "type": "string",
            "description": "Optional: just this metric of that role"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "area",
          "role"
        ]
      },
      "signature": "liveone area role clear <area> <role> [metric] { --base-url?: string }",
      "invocation": "npm run liveone -- area role clear",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__role__list",
      "summary": "The area's role→point bindings, grouped by slot.",
      "when": "",
      "parentWhen": "Reach for this when an area renders a card empty, or a metric it should have is missing —\nmost often because the device is a member but nothing binds its points to a role.",
      "description": "npm run liveone -- area role list — The area's role→point bindings, grouped by slot.\n\nAbout `role` generally: Reach for this when an area renders a card empty, or a metric it should have is missing —\nmost often because the device is a member but nothing binds its points to a role.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone area role list kew\n  liveone area role list kew --points",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "points": {
            "type": "boolean",
            "description": "Also list every bindable point on the area's devices, marking which are unbound"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "area"
        ]
      },
      "signature": "liveone area role list <area> { --base-url?: string; --points?: boolean }",
      "invocation": "npm run liveone -- area role list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__role__set",
      "summary": "Fill one (role, metric) slot — priority follows argument order.",
      "when": "Replaces THAT SLOT and leaves every other slot untouched. Naming several points sets the\nslot's whole priority order in one write, which is how a fallback chain is expressed.",
      "parentWhen": "Reach for this when an area renders a card empty, or a metric it should have is missing —\nmost often because the device is a member but nothing binds its points to a role.",
      "description": "npm run liveone -- area role set — Fill one (role, metric) slot — priority follows argument order.\n\nReplaces THAT SLOT and leaves every other slot untouched. Naming several points sets the\nslot's whole priority order in one write, which is how a fallback chain is expressed.\n\nAbout `role` generally: Reach for this when an area renders a card empty, or a metric it should have is missing —\nmost often because the device is a member but nothing binds its points to a role.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the binding (the reason says why).\n\nExamples:\n  liveone area role set kew grid rate 'amber:bidi.grid.import/rate'\n  liveone area role set kew grid rate 'amber:bidi.grid.import/rate' 'amber:bidi.grid.export/rate' --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "role": {
            "type": "string",
            "description": "e.g. grid, solar, battery, load, ev"
          },
          "metric": {
            "type": "string",
            "description": "e.g. rate, value, energy, power, proportion"
          },
          "point": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Points, highest priority first: pt_ id, logicalPath, or device:logicalPath"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "area",
          "role",
          "metric",
          "point"
        ]
      },
      "signature": "liveone area role set <area> <role> <metric> <point...> { --base-url?: string }",
      "invocation": "npm run liveone -- area role set",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__area__show",
      "summary": "An area's full aggregate: meta, members, bindings, capabilities.",
      "when": "Use this to see how an area is put together — which devices, which role→point bindings.",
      "parentWhen": "Reach for this for the SEMANTIC layer: what an area is made of and what it measured. For the\nphysical/vendor layer use `device`; for what a dashboard shows use `dashboard`.",
      "description": "npm run liveone -- area show — An area's full aggregate: meta, members, bindings, capabilities.\n\nUse this to see how an area is put together — which devices, which role→point bindings.\n\nAbout `area` generally: Reach for this for the SEMANTIC layer: what an area is made of and what it measured. For the\nphysical/vendor layer use `device`; for what a dashboard shows use `dashboard`.\n\nThe aggregate is an OBJECT, so the human rendering is the pretty-printed JSON — a table\nwould only hide its shape.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone area show daylesford\n  liveone area show ar_01kx8km3a3fh5v2csryvhskzep",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "An area: its ar_… id, integer handle, or display name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "area"
        ]
      },
      "signature": "liveone area show <area> { --base-url?: string }",
      "invocation": "npm run liveone -- area show",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__auth__list",
      "summary": "The live tokens on your account (server-side), plus this machine's logins.",
      "when": "Answers 'what can currently access my account'. --all includes dead records.",
      "parentWhen": "Run `auth login` once per machine per environment before using any domain over the API.\nEverything here manages the lo_cli_ credential; the dashboard domain USES it.",
      "description": "npm run liveone -- auth list — The live tokens on your account (server-side), plus this machine's logins.\n\nAnswers 'what can currently access my account'. --all includes dead records.\n\nAbout `auth` generally: Run `auth login` once per machine per environment before using any domain over the API.\nEverything here manages the lo_cli_ credential; the dashboard domain USES it.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone auth list\n  liveone auth list --all",
      "input_schema": {
        "type": "object",
        "properties": {
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "all": {
            "type": "boolean",
            "description": "Include revoked/expired records not yet swept"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone auth list { --base-url?: string; --all?: boolean }",
      "invocation": "npm run liveone -- auth list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__auth__login",
      "summary": "Sign in via the browser and store a token for one origin.",
      "when": "The first command to run on a new machine, and the fix for any exit-3 'not logged in'.\nUse --base-url to log in to dev/preview alongside prod.",
      "parentWhen": "Run `auth login` once per machine per environment before using any domain over the API.\nEverything here manages the lo_cli_ credential; the dashboard domain USES it.",
      "description": "npm run liveone -- auth login — Sign in via the browser and store a token for one origin.\n\nThe first command to run on a new machine, and the fix for any exit-3 'not logged in'.\nUse --base-url to log in to dev/preview alongside prod.\n\nAbout `auth` generally: Run `auth login` once per machine per environment before using any domain over the API.\nEverything here manages the lo_cli_ credential; the dashboard domain USES it.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone auth login\n  liveone auth login --base-url=http://localhost:3001 --label=dev-laptop\n  liveone auth login --no-browser",
      "input_schema": {
        "type": "object",
        "properties": {
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "label": {
            "type": "string",
            "description": "How this machine appears in `auth list` (default: hostname) (text)"
          },
          "ttl": {
            "type": "number",
            "description": "Requested token lifetime (server currently mints 90d; accepted for forward-compat) (days) — 1–365 days"
          },
          "no-browser": {
            "type": "boolean",
            "description": "Print the URL and paste the code by hand (SSH / non-mac)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone auth login { --base-url?: string; --label?: string; --ttl?: number; --no-browser?: boolean }",
      "invocation": "npm run liveone -- auth login",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__auth__logout",
      "summary": "Revoke this origin's token server-side and forget it locally.",
      "when": "Leaving a machine or handing it over. Best-effort server revoke, then local removal.",
      "parentWhen": "Run `auth login` once per machine per environment before using any domain over the API.\nEverything here manages the lo_cli_ credential; the dashboard domain USES it.",
      "description": "npm run liveone -- auth logout — Revoke this origin's token server-side and forget it locally.\n\nLeaving a machine or handing it over. Best-effort server revoke, then local removal.\n\nAbout `auth` generally: Run `auth login` once per machine per environment before using any domain over the API.\nEverything here manages the lo_cli_ credential; the dashboard domain USES it.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone auth logout",
      "input_schema": {
        "type": "object",
        "properties": {
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone auth logout { --base-url?: string }",
      "invocation": "npm run liveone -- auth logout",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__auth__revoke",
      "summary": "Revoke one token by id, or all of them.",
      "when": "Lost a machine, or `list` shows something unexpected. Revocation takes effect on the\nnext request. Revoking the token you are using also forgets it locally.",
      "parentWhen": "Run `auth login` once per machine per environment before using any domain over the API.\nEverything here manages the lo_cli_ credential; the dashboard domain USES it.",
      "description": "npm run liveone -- auth revoke — Revoke one token by id, or all of them.\n\nLost a machine, or `list` shows something unexpected. Revocation takes effect on the\nnext request. Revoking the token you are using also forgets it locally.\n\nAbout `auth` generally: Run `auth login` once per machine per environment before using any domain over the API.\nEverything here manages the lo_cli_ credential; the dashboard domain USES it.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone auth revoke cli_Rf-c6ac5\n  liveone auth revoke --all",
      "input_schema": {
        "type": "object",
        "properties": {
          "tokenId": {
            "type": "string",
            "description": "A token id from `auth list` (omit when using --all)"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "all": {
            "type": "boolean",
            "description": "Revoke every live token on the account"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone auth revoke [tokenId] { --base-url?: string; --all?: boolean }",
      "invocation": "npm run liveone -- auth revoke",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__auth__whoami",
      "summary": "Who and where the stored token makes you — the target line, on demand.",
      "when": "Run this when unsure which deployment or database a command would hit; it names the\norigin, the user, the Clerk instance and the DB host in one line.",
      "parentWhen": "Run `auth login` once per machine per environment before using any domain over the API.\nEverything here manages the lo_cli_ credential; the dashboard domain USES it.",
      "description": "npm run liveone -- auth whoami — Who and where the stored token makes you — the target line, on demand.\n\nRun this when unsure which deployment or database a command would hit; it names the\norigin, the user, the Clerk instance and the DB host in one line.\n\nAbout `auth` generally: Run `auth login` once per machine per environment before using any domain over the API.\nEverything here manages the lo_cli_ credential; the dashboard domain USES it.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone auth whoami\n  liveone auth whoami --base-url=http://localhost:3001",
      "input_schema": {
        "type": "object",
        "properties": {
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone auth whoami { --base-url?: string }",
      "invocation": "npm run liveone -- auth whoami",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__automation__create-exercise",
      "summary": "Schedule a generator exercise run — unless it has already run under load recently.",
      "when": "Reach for this for anti-wet-stacking: a diesel that idles for weeks glazes its bores. The\nrule fires on a weekly wall-clock slot and SKIPS itself when the engine has already done\nreal work, so a generator in normal use is never exercised unnecessarily.",
      "parentWhen": "Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.",
      "description": "npm run liveone -- automation create-exercise — Schedule a generator exercise run — unless it has already run under load recently.\n\nReach for this for anti-wet-stacking: a diesel that idles for weeks glazes its bores. The\nrule fires on a weekly wall-clock slot and SKIPS itself when the engine has already done\nreal work, so a generator in normal use is never exercised unnecessarily.\n\nAbout `automation` generally: Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.\n\n🛑 This creates something that STARTS AN ENGINE, on a schedule, unattended. Dry-run is the\ndefault; read the printed rule before `--apply`.\n\nThree points are involved and they are not interchangeable:\n  --derivation    the run detector, which answers 'is it running' and 'did it run'\n  --load-point    a power point in WATTS (negative = import) that says how HARD it ran;\n                  the DeepSea controller has no CTs, so load is read from the inverter\n  --action-point  the writable run-request point the run is commanded through\n\nA point is a pt_… id, a logical path on one of the AREA's devices, or the qualified form\n`<device>:<path>`. The qualified form is not a nicety: only the DERIVATION has to live in\nthe area, so the run-request point is routinely on a device that is not a member of it.\n\nThe rule never dispatches while a run is already in progress: a second request would\nrecompute the hub's stop deadline from now and truncate the run someone else asked for.\n\nTimes are the AREA's local wall clock and stay that way across daylight saving.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the rule (422) — nothing was written.\n\nExamples:\n  liveone automation create-exercise daylesford --derivation=generator --load-point=bidi.grid/power --action-point='Daylesford Generator':source.generator.control.request/duration --weekdays=thu --time=09:00 --minutes=30",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "The area: ar_… id, legacy handle, slug or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "derivation": {
            "type": "string",
            "description": "The run detector: dx_… id, its name, or its role (e.g. generator) (dx_|role)"
          },
          "load-point": {
            "type": "string",
            "description": "Power point in W used to judge load (e.g. bidi.grid/power, or dev:bidi.grid/power) (path|pt_)"
          },
          "action-point": {
            "type": "string",
            "description": "Writable run-request point, often on another device (e.g. generator:source.generator.control.request/duration) (path|pt_)"
          },
          "weekdays": {
            "type": "string",
            "description": "Comma-separated: sun, mon, tue, wed, thu, fri, sat (thu)"
          },
          "time": {
            "type": "string",
            "description": "24-hour local wall-clock start time (not 02:00–02:59) (09:00)"
          },
          "minutes": {
            "type": "number",
            "description": "How long to run for. Must be > 0 — 0 is a STOP, not a run (30)"
          },
          "name": {
            "type": "string",
            "description": "Name (default: 'Generator exercise')"
          },
          "grace-minutes": {
            "type": "number",
            "description": "How long a missed slot stays due before it is written off (default 180)"
          },
          "min-minutes": {
            "type": "number",
            "description": "Continuous loaded minutes that count as already exercised (default 30)"
          },
          "min-load-kw": {
            "type": "number",
            "description": "Load floor in kW — an idle run does not clear wet stacking (default 1.5)"
          },
          "dip-seconds": {
            "type": "number",
            "description": "Brief sub-threshold dips bridged rather than splitting a stretch (default 180)"
          },
          "within-days": {
            "type": "number",
            "description": "How far back to look for such a run (default 7)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "area",
          "derivation",
          "load-point",
          "action-point",
          "weekdays",
          "time",
          "minutes"
        ]
      },
      "signature": "liveone automation create-exercise <area> { --base-url?: string; --derivation: string; --load-point: string; --action-point: string; --weekdays: string; --tim …",
      "invocation": "npm run liveone -- automation create-exercise",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__automation__delete",
      "summary": "Delete an automation.",
      "when": "Permanent. `disable` is the reversible option and is almost always the one you want.",
      "parentWhen": "Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.",
      "description": "npm run liveone -- automation delete — Delete an automation.\n\nPermanent. `disable` is the reversible option and is almost always the one you want.\n\nAbout `automation` generally: Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "The area: ar_… id, legacy handle, slug or name"
          },
          "automation": {
            "type": "string",
            "description": "The automation: au_… id or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "area",
          "automation"
        ]
      },
      "signature": "liveone automation delete <area> <automation> { --base-url?: string }",
      "invocation": "npm run liveone -- automation delete",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__automation__disable",
      "summary": "Stop an automation being evaluated, without deleting it.",
      "when": "The reversible way to park a rule — useful before site work, when an unattended engine\nstart would be unwelcome.",
      "parentWhen": "Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.",
      "description": "npm run liveone -- automation disable — Stop an automation being evaluated, without deleting it.\n\nThe reversible way to park a rule — useful before site work, when an unattended engine\nstart would be unwelcome.\n\nAbout `automation` generally: Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "The area: ar_… id, legacy handle, slug or name"
          },
          "automation": {
            "type": "string",
            "description": "The automation: au_… id or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "area",
          "automation"
        ]
      },
      "signature": "liveone automation disable <area> <automation> { --base-url?: string }",
      "invocation": "npm run liveone -- automation disable",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__automation__enable",
      "summary": "Re-enable a disabled automation.",
      "when": "Re-enabling an exercise rule inside a slot it has already dealt with does NOT give it a\nsecond chance to fire — the consumed-slot key survives the toggle, by design.",
      "parentWhen": "Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.",
      "description": "npm run liveone -- automation enable — Re-enable a disabled automation.\n\nRe-enabling an exercise rule inside a slot it has already dealt with does NOT give it a\nsecond chance to fire — the consumed-slot key survives the toggle, by design.\n\nAbout `automation` generally: Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "The area: ar_… id, legacy handle, slug or name"
          },
          "automation": {
            "type": "string",
            "description": "The automation: au_… id or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "area",
          "automation"
        ]
      },
      "signature": "liveone automation enable <area> <automation> { --base-url?: string }",
      "invocation": "npm run liveone -- automation enable",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__automation__list",
      "summary": "Every automation on an area, and what each one does.",
      "when": "The first call — `au_` ids are per-environment, so start here before any other verb.",
      "parentWhen": "Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.",
      "description": "npm run liveone -- automation list — Every automation on an area, and what each one does.\n\nThe first call — `au_` ids are per-environment, so start here before any other verb.\n\nAbout `automation` generally: Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.\n\nOne line each: id, enabled, a summary of the trigger, name. A row whose stored trigger\ncould not be parsed shows UNREADABLE rather than a guess — it is still listable, and still\ndeletable, which is the point.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the area has no automations.\n\nExamples:\n  liveone automation list daylesford",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "The area: ar_… id, legacy handle, slug or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "area"
        ]
      },
      "signature": "liveone automation list <area> { --base-url?: string }",
      "invocation": "npm run liveone -- automation list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__automation__show",
      "summary": "One automation in full, including the last decision the evaluator made.",
      "when": "Reach for this to answer 'why didn't it run last Thursday'. For an exercise rule the\noutcome, the reason and the load evidence are all recorded on the row.",
      "parentWhen": "Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.",
      "description": "npm run liveone -- automation show — One automation in full, including the last decision the evaluator made.\n\nReach for this to answer 'why didn't it run last Thursday'. For an exercise rule the\noutcome, the reason and the load evidence are all recorded on the row.\n\nAbout `automation` generally: Reach for this to see or change what fires by itself. For what a run DETECTOR measures use\n`derivation`; automations act on what those detectors report.\n\nThere is no GET-by-id route, so the automation is resolved from the area's list — which is\nwhy this verb takes the area as well.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone automation show daylesford 'Generator exercise'",
      "input_schema": {
        "type": "object",
        "properties": {
          "area": {
            "type": "string",
            "description": "The area: ar_… id, legacy handle, slug or name"
          },
          "automation": {
            "type": "string",
            "description": "The automation: au_… id or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "area",
          "automation"
        ]
      },
      "signature": "liveone automation show <area> <automation> { --base-url?: string }",
      "invocation": "npm run liveone -- automation show",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__add-card",
      "summary": "Insert a card node.",
      "when": "Use this to put a new card on a dashboard; `add-group` makes a container instead.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard add-card — Insert a card node.\n\nUse this to put a new card on a dashboard; `add-group` makes a container instead.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard add-card kink --type=heatmap --device=dv_01kybrhzkmfyxvz63d15rscj19 --after=n_2VF4\n  liveone dashboard add-card kink --type=chart --config-json='{\"variant\":\"lines\"}' --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "type": {
            "type": "string",
            "description": "The card type, e.g. solar, chart, heatmap (cardType)"
          },
          "config-json": {
            "type": "string",
            "description": "The card's config, inline (json)"
          },
          "config-file": {
            "type": "string",
            "description": "The card's config, from a JSON file (path)"
          },
          "allow-unknown-type": {
            "type": "boolean",
            "description": "Write a type this build does not know (it renders as a placeholder)"
          },
          "area": {
            "type": "string",
            "description": "Bind the node to an area (scope-bearing; readability is NOT checked) (ar_id)"
          },
          "device": {
            "type": "string",
            "description": "Bind the node to a device (scope-bearing; readability is NOT checked) (dv_id)"
          },
          "hidden": {
            "type": "boolean",
            "description": "Mark the node hidden"
          },
          "columns": {
            "type": "number",
            "description": "Width hint (1-12) — 1–12 on the 12-column grid"
          },
          "parent": {
            "type": "string",
            "description": "Insert inside this group (default: the root) (n_id)"
          },
          "index": {
            "type": "number",
            "description": "Position within the parent (default: append) (k) — 0-based position among the parent's children"
          },
          "before": {
            "type": "string",
            "description": "Insert immediately before this sibling (n_id)"
          },
          "after": {
            "type": "string",
            "description": "Insert immediately after this sibling (n_id)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash",
          "type"
        ]
      },
      "signature": "liveone dashboard add-card <dash> { --via?: \"http\"|\"db\"; --base-url?: string; --type: string; --config-json?: string; --config-file?: string; --allow-unknown- …",
      "invocation": "npm run liveone -- dashboard add-card",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__add-group",
      "summary": "Insert an empty group node.",
      "when": "Use this for structure — a row of tiles, or an area section. A group with an area and\nheading is what used to be called a section; a row group is what used to be a tiles card.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard add-group — Insert an empty group node.\n\nUse this for structure — a row of tiles, or an area section. A group with an area and\nheading is what used to be called a section; a row group is what used to be a tiles card.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard add-group kink --direction=row --wrap --after=n_CBEX\n  liveone dashboard add-group kink --area=ar_01kx8km3a3fh5v2csryvhskzep --heading --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "direction": {
            "type": "string",
            "description": "Flex direction (default: column)",
            "enum": [
              "row",
              "column"
            ]
          },
          "wrap": {
            "type": "boolean",
            "description": "Allow children to wrap"
          },
          "heading": {
            "type": "boolean",
            "description": "Render the bound area's header"
          },
          "area": {
            "type": "string",
            "description": "Bind the node to an area (scope-bearing; readability is NOT checked) (ar_id)"
          },
          "device": {
            "type": "string",
            "description": "Bind the node to a device (scope-bearing; readability is NOT checked) (dv_id)"
          },
          "hidden": {
            "type": "boolean",
            "description": "Mark the node hidden"
          },
          "columns": {
            "type": "number",
            "description": "Width hint (1-12) — 1–12 on the 12-column grid"
          },
          "parent": {
            "type": "string",
            "description": "Insert inside this group (default: the root) (n_id)"
          },
          "index": {
            "type": "number",
            "description": "Position within the parent (default: append) (k) — 0-based position among the parent's children"
          },
          "before": {
            "type": "string",
            "description": "Insert immediately before this sibling (n_id)"
          },
          "after": {
            "type": "string",
            "description": "Insert immediately after this sibling (n_id)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash"
        ]
      },
      "signature": "liveone dashboard add-group <dash> { --via?: \"http\"|\"db\"; --base-url?: string; --direction?: \"row\"|\"column\"; --wrap?: boolean; --heading?: boolean; --area?: s …",
      "invocation": "npm run liveone -- dashboard add-group",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__backfill-history",
      "summary": "Seed a history row for every dashboard whose current revision has none.",
      "when": "Run ONCE per environment after the revisions writers land, so `restore` has a floor for\ndocuments that predate them. Idempotent — a dashboard already recorded is skipped.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard backfill-history — Seed a history row for every dashboard whose current revision has none.\n\nRun ONCE per environment after the revisions writers land, so `restore` has a floor for\ndocuments that predate them. Idempotent — a dashboard already recorded is skipped.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\ndb transport only: it writes rows the API deliberately has no endpoint for (history is\nserver-written, not client-supplied). Rows are inserted with savedBy=backfill at each\ndashboard's CURRENT revision, ON CONFLICT DO NOTHING.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  npm run liveone:dev -- dashboard backfill-history\n  liveone dashboard backfill-history --via=db --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        }
      },
      "signature": "liveone dashboard backfill-history { --via?: \"http\"|\"db\"; --base-url?: string }",
      "invocation": "npm run liveone -- dashboard backfill-history",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__delete",
      "summary": "Delete a dashboard — and every grant and share link on it.",
      "when": "🛑 IRREVERSIBLE, and wider than it looks. `dashboard_grants`, `share_tokens` and the whole\nrevision history all cascade from `dashboards.id`, so deleting a document silently revokes\nevery live link on it. The dry run names them before you commit to that.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard delete — Delete a dashboard — and every grant and share link on it.\n\n🛑 IRREVERSIBLE, and wider than it looks. `dashboard_grants`, `share_tokens` and the whole\nrevision history all cascade from `dashboards.id`, so deleting a document silently revokes\nevery live link on it. The dry run names them before you commit to that.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nA share token's SCOPE comes from the doc's refs, so a card-less dashboard can still be\nconveying read access to whole areas — which is exactly the kind that looks safe to delete\nand is the most important to look at first.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused (the reason says why).\n\nExamples:\n  liveone dashboard delete legacy-share-keen-fruity-tapir\n  liveone dashboard delete legacy-share-keen-fruity-tapir --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash"
        ]
      },
      "signature": "liveone dashboard delete <dash> { --base-url?: string }",
      "invocation": "npm run liveone -- dashboard delete",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__duplicate",
      "summary": "Copy a dashboard to a NEW one — same cards, fresh node ids.",
      "when": "Use this to fork a dashboard before restyling it, instead of hand-rolling a POST. The\ncopy is created through the API's full validation and slug rules.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard duplicate — Copy a dashboard to a NEW one — same cards, fresh node ids.\n\nUse this to fork a dashboard before restyling it, instead of hand-rolling a POST. The\ncopy is created through the API's full validation and slug rules.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nhttp transport only. The server re-mints every node id, so n_… ids noted from the SOURCE\ndo not address nodes in the copy — run `show` on the copy before editing it.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard duplicate daylesford --name=\"Daylesford (stacked)\" --slug=daylesford-stacked\n  liveone dashboard duplicate kink --name=\"Kinkora copy\" --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "name": {
            "type": "string",
            "description": "Display name for the copy (text)"
          },
          "slug": {
            "type": "string",
            "description": "Owner-unique shortname for the copy (omit for none) (kebab)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash",
          "name"
        ]
      },
      "signature": "liveone dashboard duplicate <dash> { --via?: \"http\"|\"db\"; --base-url?: string; --name: string; --slug?: string }",
      "invocation": "npm run liveone -- dashboard duplicate",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__history",
      "summary": "The dashboard's edit history — who changed it, when, revision by revision.",
      "when": "Run this before `restore`, and any time an edit surprises you. Every write records a\npost-image row, so revision N here IS version N of the document.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard history — The dashboard's edit history — who changed it, when, revision by revision.\n\nRun this before `restore`, and any time an edit surprises you. Every write records a\npost-image row, so revision N here IS version N of the document.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nsavedBy is a provenance string, not always a person: routes record the caller's Clerk\nuserId, the CLI records `cli`, scripts `script:<name>`, and the backfill `backfill`.\nHistory is per-environment — the prod→dev sync deliberately does not carry it.\n\nRead-only. This command changes nothing.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = no history recorded (run backfill-history).\n\nExamples:\n  liveone dashboard history kink\n  liveone dashboard history kink --limit=5",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "limit": {
            "type": "number",
            "description": "How many revisions to show, newest first — 1–500",
            "default": 20
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "dash"
        ]
      },
      "signature": "liveone dashboard history <dash> { --via?: \"http\"|\"db\"; --base-url?: string; --limit?: number }",
      "invocation": "npm run liveone -- dashboard history",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__link__create",
      "summary": "Mint a new share link.",
      "when": "",
      "parentWhen": "A link lets someone with the URL read the dashboard WITHOUT signing in. For a named user\nuse `dashboard share`.",
      "description": "npm run liveone -- dashboard link create — Mint a new share link.\n\nAbout `link` generally: A link lets someone with the URL read the dashboard WITHOUT signing in. For a named user\nuse `dashboard share`.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused (the reason says why).\n\nExamples:\n  liveone dashboard link create kew --label='for the installer' --expires-in-days=30 --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "label": {
            "type": "string",
            "description": "What this link is for — the only way to tell two links apart later (text)"
          },
          "expires-in-days": {
            "type": "number",
            "description": "Expire after N days (default: never) (30)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash"
        ]
      },
      "signature": "liveone dashboard link create <dash> { --base-url?: string; --label?: string; --expires-in-days?: number }",
      "invocation": "npm run liveone -- dashboard link create",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__link__list",
      "summary": "The dashboard's links: label, created, expiry, last use, revoked.",
      "when": "",
      "parentWhen": "A link lets someone with the URL read the dashboard WITHOUT signing in. For a named user\nuse `dashboard share`.",
      "description": "npm run liveone -- dashboard link list — The dashboard's links: label, created, expiry, last use, revoked.\n\nAbout `link` generally: A link lets someone with the URL read the dashboard WITHOUT signing in. For a named user\nuse `dashboard share`.\n\nRead-only. This command changes nothing.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard link list kew",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "dash"
        ]
      },
      "signature": "liveone dashboard link list <dash> { --base-url?: string }",
      "invocation": "npm run liveone -- dashboard link list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__link__revoke",
      "summary": "Revoke a share link. Idempotent.",
      "when": "",
      "parentWhen": "A link lets someone with the URL read the dashboard WITHOUT signing in. For a named user\nuse `dashboard share`.",
      "description": "npm run liveone -- dashboard link revoke — Revoke a share link. Idempotent.\n\nAbout `link` generally: A link lets someone with the URL read the dashboard WITHOUT signing in. For a named user\nuse `dashboard share`.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused (the reason says why).\n\nExamples:\n  liveone dashboard link revoke kew abc123 --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "token": {
            "type": "string",
            "description": "The token to revoke"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash",
          "token"
        ]
      },
      "signature": "liveone dashboard link revoke <dash> <token> { --base-url?: string }",
      "invocation": "npm run liveone -- dashboard link revoke",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__list",
      "summary": "List dashboards: id, owner, name, slug, revision and card count.",
      "when": "Start here when you do not yet know a dashboard's id.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard list — List dashboards: id, owner, name, slug, revision and card count.\n\nStart here when you do not yet know a dashboard's id.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nRead-only. This command changes nothing.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard list\n  liveone dashboard list --format json",
      "input_schema": {
        "type": "object",
        "properties": {
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "owner": {
            "type": "string",
            "description": "Only this owner's dashboards (userId)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone dashboard list { --via?: \"http\"|\"db\"; --base-url?: string; --owner?: string }",
      "invocation": "npm run liveone -- dashboard list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__move-node",
      "summary": "Move a node, subtree intact and ids preserved.",
      "when": "Use this to reorder or re-parent. Ids survive the move, so a later edit can still\naddress the node by the id `show` printed before it.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard move-node — Move a node, subtree intact and ids preserved.\n\nUse this to reorder or re-parent. Ids survive the move, so a later edit can still\naddress the node by the id `show` printed before it.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard move-node kink n_FS02 --before=n_E7Z1\n  liveone dashboard move-node kink n_FS02 --parent=n_CBEX --index=0 --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "node": {
            "type": "string",
            "description": "The n_… id of the node, as printed by `show`"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "parent": {
            "type": "string",
            "description": "Insert inside this group (default: the root) (n_id)"
          },
          "index": {
            "type": "number",
            "description": "Position within the parent (default: append) (k) — 0-based position among the parent's children"
          },
          "before": {
            "type": "string",
            "description": "Insert immediately before this sibling (n_id)"
          },
          "after": {
            "type": "string",
            "description": "Insert immediately after this sibling (n_id)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash",
          "node"
        ]
      },
      "signature": "liveone dashboard move-node <dash> <node> { --via?: \"http\"|\"db\"; --base-url?: string; --parent?: string; --index?: number; --before?: string; --after?: string }",
      "invocation": "npm run liveone -- dashboard move-node",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__remint-ids",
      "summary": "Re-mint every node id in a document (one-time migration).",
      "when": "A MIGRATION, not an edit: run it once per document to move ids off the retired sequential\nform (n_0, n_1, …) onto the random form. Every id changes, so any id noted from an earlier\n`show` stops resolving — which is the point, because a sequential id could be RECYCLED onto\na different node after a removal. Structure, refs and config are untouched.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard remint-ids — Re-mint every node id in a document (one-time migration).\n\nA MIGRATION, not an edit: run it once per document to move ids off the retired sequential\nform (n_0, n_1, …) onto the random form. Every id changes, so any id noted from an earlier\n`show` stops resolving — which is the point, because a sequential id could be RECYCLED onto\na different node after a removal. Structure, refs and config are untouched.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard remint-ids db_01kyf18tp3e5brm474zf0fzvkm\n  liveone dashboard remint-ids db_01kyf18tp3e5brm474zf0fzvkm --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash"
        ]
      },
      "signature": "liveone dashboard remint-ids <dash> { --via?: \"http\"|\"db\"; --base-url?: string }",
      "invocation": "npm run liveone -- dashboard remint-ids",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__remove-node",
      "summary": "Remove a node and its whole subtree.",
      "when": "Removes the node AND everything under it — check `show` first if it is a group.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard remove-node — Remove a node and its whole subtree.\n\nRemoves the node AND everything under it — check `show` first if it is a group.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard remove-node kink n_5CKF\n  liveone dashboard remove-node kink n_5CKF --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "node": {
            "type": "string",
            "description": "The n_… id of the node, as printed by `show`"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash",
          "node"
        ]
      },
      "signature": "liveone dashboard remove-node <dash> <node> { --via?: \"http\"|\"db\"; --base-url?: string }",
      "invocation": "npm run liveone -- dashboard remove-node",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__rename",
      "summary": "Change a dashboard's name and/or slug. Metadata only — the doc is untouched.",
      "when": "Use this for the dashboard's own name or its /dashboard/{user}/{slug} shortname.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard rename — Change a dashboard's name and/or slug. Metadata only — the doc is untouched.\n\nUse this for the dashboard's own name or its /dashboard/{user}/{slug} shortname.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard rename kink --slug=kinkora\n  liveone dashboard rename kink --name='Kinkora' --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "name": {
            "type": "string",
            "description": "New display name, or \"none\" to clear it (text)"
          },
          "slug": {
            "type": "string",
            "description": "New owner-unique shortname, or \"none\" to clear it (kebab)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash"
        ]
      },
      "signature": "liveone dashboard rename <dash> { --via?: \"http\"|\"db\"; --base-url?: string; --name?: string; --slug?: string }",
      "invocation": "npm run liveone -- dashboard rename",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__restore",
      "summary": "Restore a recorded revision — as a NEW revision, never a counter rewind.",
      "when": "The undo. Find the revision with `history`, preview the restore dry, then --apply. The\nrestore itself is recorded, so history shows what happened and is itself restorable.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard restore — Restore a recorded revision — as a NEW revision, never a counter rewind.\n\nThe undo. Find the revision with `history`, preview the restore dry, then --apply. The\nrestore itself is recorded, so history shows what happened and is itself restorable.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nThe recorded doc is re-validated against TODAY'S card vocabulary before writing — a\nmonths-old snapshot may name a type this build no longer knows, and restoring it blindly\nwould write a grey box. A doc that no longer validates is refused with its issues.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard restore kink --revision=3\n  liveone dashboard restore kink --revision=3 --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "revision": {
            "type": "number",
            "description": "The recorded revision to restore — a revision number from `history`"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash",
          "revision"
        ]
      },
      "signature": "liveone dashboard restore <dash> { --via?: \"http\"|\"db\"; --base-url?: string; --revision: number }",
      "invocation": "npm run liveone -- dashboard restore",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__set-prop",
      "summary": "Set or clear a node's envelope props, and a card's type/config.",
      "when": "Use this to change an existing node in place — bind it to a different device, resize it,\nhide it, or replace a card's config. Pass `none` to any property to DELETE that key.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard set-prop — Set or clear a node's envelope props, and a card's type/config.\n\nUse this to change an existing node in place — bind it to a different device, resize it,\nhide it, or replace a card's config. Pass `none` to any property to DELETE that key.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard set-prop kink n_VX15 --columns=6\n  liveone dashboard set-prop kink n_VX15 --hidden=none --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "node": {
            "type": "string",
            "description": "The n_… id of the node, as printed by `show`"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "area": {
            "type": "string",
            "description": "Bind to an area, or none to clear (readability is NOT checked) (ar_id|none)"
          },
          "device": {
            "type": "string",
            "description": "Bind to a device, or none to clear (readability is NOT checked) (dv_id|none)"
          },
          "hidden": {
            "type": "string",
            "description": "Set or clear the hidden flag",
            "enum": [
              "true",
              "false",
              "none"
            ]
          },
          "wrap": {
            "type": "string",
            "description": "Group only: set or clear wrapping",
            "enum": [
              "true",
              "false",
              "none"
            ]
          },
          "heading": {
            "type": "string",
            "description": "Group only: set or clear the area header",
            "enum": [
              "true",
              "false",
              "none"
            ]
          },
          "direction": {
            "type": "string",
            "description": "Group only: flex direction, or none to clear",
            "enum": [
              "row",
              "column",
              "none"
            ]
          },
          "columns": {
            "type": "string",
            "description": "Width hint, or none to clear (1-12|none)"
          },
          "type": {
            "type": "string",
            "description": "Card only: change the card type (cardType)"
          },
          "config": {
            "type": "string",
            "description": "Card only: clear the config (use --config-json/--config-file to set)",
            "enum": [
              "none"
            ]
          },
          "config-json": {
            "type": "string",
            "description": "Card only: replace the config, inline (json)"
          },
          "config-file": {
            "type": "string",
            "description": "Card only: replace the config, from a JSON file (path)"
          },
          "allow-unknown-type": {
            "type": "boolean",
            "description": "Allow a --type this build does not know"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash",
          "node"
        ]
      },
      "signature": "liveone dashboard set-prop <dash> <node> { --via?: \"http\"|\"db\"; --base-url?: string; --area?: string; --device?: string; --hidden?: \"true\"|\"false\"|\"none\"; --w …",
      "invocation": "npm run liveone -- dashboard set-prop",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__share__add",
      "summary": "Grant users on a dashboard, keeping existing members.",
      "when": "The incremental verb. The route is a full replace, so this reads the membership first —\nsending one member alone would evict everyone else.",
      "parentWhen": "A grant is how a NAMED user sees a dashboard — and, through it, the devices the doc\nreferences. For an anonymous link use `dashboard link`.",
      "description": "npm run liveone -- dashboard share add — Grant users on a dashboard, keeping existing members.\n\nThe incremental verb. The route is a full replace, so this reads the membership first —\nsending one member alone would evict everyone else.\n\nAbout `share` generally: A grant is how a NAMED user sees a dashboard — and, through it, the devices the doc\nreferences. For an anonymous link use `dashboard link`.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the membership (the reason says why).\n\nExamples:\n  liveone dashboard share add kew karoline@example.com\n  liveone dashboard share add kew karoline@example.com --role=admin --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "user": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Users: user_… id, email, or username"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "role": {
            "type": "string",
            "description": "Grant role (default: viewer) (viewer)",
            "enum": [
              "viewer",
              "admin"
            ]
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash",
          "user"
        ]
      },
      "signature": "liveone dashboard share add <dash> <user...> { --base-url?: string; --role?: \"viewer\"|\"admin\" }",
      "invocation": "npm run liveone -- dashboard share add",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__share__list",
      "summary": "The dashboard's members, and each one's grant role.",
      "when": "",
      "parentWhen": "A grant is how a NAMED user sees a dashboard — and, through it, the devices the doc\nreferences. For an anonymous link use `dashboard link`.",
      "description": "npm run liveone -- dashboard share list — The dashboard's members, and each one's grant role.\n\nAbout `share` generally: A grant is how a NAMED user sees a dashboard — and, through it, the devices the doc\nreferences. For an anonymous link use `dashboard link`.\n\nRead-only. This command changes nothing.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard share list kew",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "dash"
        ]
      },
      "signature": "liveone dashboard share list <dash> { --base-url?: string }",
      "invocation": "npm run liveone -- dashboard share list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__share__remove",
      "summary": "Revoke users from a dashboard, keeping the rest.",
      "when": "",
      "parentWhen": "A grant is how a NAMED user sees a dashboard — and, through it, the devices the doc\nreferences. For an anonymous link use `dashboard link`.",
      "description": "npm run liveone -- dashboard share remove — Revoke users from a dashboard, keeping the rest.\n\nAbout `share` generally: A grant is how a NAMED user sees a dashboard — and, through it, the devices the doc\nreferences. For an anonymous link use `dashboard link`.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the membership (the reason says why).\n\nExamples:\n  liveone dashboard share remove kew someone@example.com --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "user": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Users to revoke"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash",
          "user"
        ]
      },
      "signature": "liveone dashboard share remove <dash> <user...> { --base-url?: string }",
      "invocation": "npm run liveone -- dashboard share remove",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__share__set",
      "summary": "Declare the exact membership — anyone omitted is revoked.",
      "when": "The full-replace form, matching the route. Prefer `add`/`remove` unless you mean 'these\nand only these'.",
      "parentWhen": "A grant is how a NAMED user sees a dashboard — and, through it, the devices the doc\nreferences. For an anonymous link use `dashboard link`.",
      "description": "npm run liveone -- dashboard share set — Declare the exact membership — anyone omitted is revoked.\n\nThe full-replace form, matching the route. Prefer `add`/`remove` unless you mean 'these\nand only these'.\n\nAbout `share` generally: A grant is how a NAMED user sees a dashboard — and, through it, the devices the doc\nreferences. For an anonymous link use `dashboard link`.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the membership (the reason says why).\n\nExamples:\n  liveone dashboard share set kew simon@example.com --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "user": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The complete membership (none = revoke everyone)"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "role": {
            "type": "string",
            "description": "Grant role for everyone named (default: viewer) (viewer)",
            "enum": [
              "viewer",
              "admin"
            ]
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "dash"
        ]
      },
      "signature": "liveone dashboard share set <dash> [user...] { --base-url?: string; --role?: \"viewer\"|\"admin\" }",
      "invocation": "npm run liveone -- dashboard share set",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__show",
      "summary": "Render a dashboard's node tree, with the n_… ids edits address.",
      "when": "Run this before any edit: the n_… ids it prints are the handles every other subcommand\ntakes, and they are per-environment so they must be read from the database you intend\nto change.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard show — Render a dashboard's node tree, with the n_… ids edits address.\n\nRun this before any edit: the n_… ids it prints are the handles every other subcommand\ntakes, and they are per-environment so they must be read from the database you intend\nto change.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nAlways renders the NORMALIZED document, so the ids shown are the ids a write would\npersist. --format json emits the same normalized doc (or one subtree with --node).\n\nRead-only. This command changes nothing.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone dashboard show kink\n  liveone dashboard show db_01kyf18tp3e5brm474zf0fzvkm --node=n_2XRX",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "node": {
            "type": "string",
            "description": "Render only this node's subtree (n_id)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "dash"
        ]
      },
      "signature": "liveone dashboard show <dash> { --via?: \"http\"|\"db\"; --base-url?: string; --node?: string }",
      "invocation": "npm run liveone -- dashboard show",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__dashboard__validate",
      "summary": "Validate a stored dashboard doc, or a doc in a JSON file.",
      "when": "Use this to find out WHY an edit is being refused, or to check a document you are about\nto write from a file.",
      "parentWhen": "Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.",
      "description": "npm run liveone -- dashboard validate — Validate a stored dashboard doc, or a doc in a JSON file.\n\nUse this to find out WHY an edit is being refused, or to check a document you are about\nto write from a file.\n\nAbout `dashboard` generally: Reach for this to read or change what a dashboard SHOWS — its cards, groups, layout and\nwhich area or device each is bound to — directly in the database. For a card-type rename\nacross every stored document use `scripts/utils/migrate-card-type.ts` instead; for the\nareas and devices a dashboard refers to, edit those through the app.\n\nRead-only. This command changes nothing.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the document is invalid.\n\nExamples:\n  liveone dashboard validate kink\n  liveone dashboard validate --file=doc.json",
      "input_schema": {
        "type": "object",
        "properties": {
          "dash": {
            "type": "string",
            "description": "A dashboard: its db_… id or its slug"
          },
          "via": {
            "type": "string",
            "description": "How to reach the data: the deployed API (http) or Postgres directly (db)",
            "enum": [
              "http",
              "db"
            ],
            "default": "http"
          },
          "base-url": {
            "type": "string",
            "description": "http only: target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "file": {
            "type": "string",
            "description": "Validate this JSON file instead of a stored dashboard (path)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone dashboard validate [dash] { --via?: \"http\"|\"db\"; --base-url?: string; --file?: string }",
      "invocation": "npm run liveone -- dashboard validate",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__derivation__create",
      "summary": "Add a derivation to a device.",
      "when": "Use this to start tracking a device's runs — a generator, or an EV charger. For the HWS\nthermal model pass --kind=hws-model, which takes no points (it finds its own).",
      "parentWhen": "Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.",
      "description": "npm run liveone -- derivation create — Add a derivation to a device.\n\nUse this to start tracking a device's runs — a generator, or an EV charger. For the HWS\nthermal model pass --kind=hws-model, which takes no points (it finds its own).\n\nAbout `derivation` generally: Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.\n\n<device> is what the detector is ABOUT: it is the scope --signal/--energy resolve in, and\n(via its points) what the derivation ends up addressed by. It is not a placement — the\nserver derives the site from the wiring either way, so naming a device whose points you\nthen do not use simply resolves nothing.\n\n--signal/--energy take a LOGICAL PATH (`load.ev/power`) on <device>, a qualified\n`<other-device>:<path>` for the cross-device case, or a pt_… id; prefer a path, since a\nmis-pinned uuid fails silently — the detector simply never fires. Write access is required\non EVERY device the points land on, not just one of them.\n\nOne of --upper/--lower is required: they are the threshold the run is detected against.\nEvery OTHER knob is sparse by contract — a flag you do not pass is not written, so it\ninherits the role's default (lib/run-tracking/defaults.ts) as those defaults evolve.\nPassing a value equal to today's default is therefore NOT a no-op; it pins it.\n\n--energy is optional and its absence is legal: a detector with no cumulative energy\ncounter still records duration and signal statistics, and the runs card drops the kWh,\ncost, emissions and renewable columns rather than showing them empty.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the derivation (the reason says why).\n\nExamples:\n  liveone derivation create kutis --role=ev --name='EV charging' --signal=load.ev/power --upper=100 --delay-off=300\n  liveone derivation create kutis --role=ev --signal=load.ev/power --upper=100 --apply\n  liveone derivation create 14 --role=generator --signal=source.generator/power --energy=1:source.generator/energy.total --upper=500 --apply\n  liveone derivation create kink --kind=hws-model --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "The device the derivation is about: its dv_… id, integer handle, slug, or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "kind": {
            "type": "string",
            "description": "What sort of derived signal",
            "enum": [
              "run-detector",
              "hws-model"
            ],
            "default": "run-detector"
          },
          "role": {
            "type": "string",
            "description": "run-detector only: which role's runs these are",
            "enum": [
              "generator",
              "ev"
            ]
          },
          "name": {
            "type": "string",
            "description": "Display name (default: '<role> runs') (text)"
          },
          "signal": {
            "type": "string",
            "description": "run-detector only: the series to follow, e.g. load.ev/power (path|pt_)"
          },
          "energy": {
            "type": "string",
            "description": "Optional cumulative energy counter, for per-run kWh (path|pt_)"
          },
          "upper": {
            "type": "number",
            "description": "Above this, the device is ON (W)"
          },
          "lower": {
            "type": "number",
            "description": "Below this, the device is OFF (W)"
          },
          "hysteresis": {
            "type": "number",
            "description": "Deadband around the threshold (W)"
          },
          "delay-on": {
            "type": "number",
            "description": "Ignore an on-signal shorter than this (seconds)"
          },
          "delay-off": {
            "type": "number",
            "description": "Bridge a gap shorter than this. 🛑 Must comfortably EXCEED the point's sample interval, or every poll gap closes a run (seconds)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "device"
        ]
      },
      "signature": "liveone derivation create <device> { --base-url?: string; --kind?: \"run-detector\"|\"hws-model\"; --role?: \"generator\"|\"ev\"; --name?: string; --signal?: string; …",
      "invocation": "npm run liveone -- derivation create",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__derivation__delete",
      "summary": "Destroy a derivation, and every interval it ever produced.",
      "when": "Use this only for a detector that should never have existed — a duplicate, or one wired to\nthe wrong device. To stop a detector you still want the history of, use `disable`.",
      "parentWhen": "Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.",
      "description": "npm run liveone -- derivation delete — Destroy a derivation, and every interval it ever produced.\n\nUse this only for a detector that should never have existed — a duplicate, or one wired to\nthe wrong device. To stop a detector you still want the history of, use `disable`.\n\nAbout `derivation` generally: Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.\n\n🛑 `derived_intervals` CASCADEs: deleting a derivation destroys every run it ever recorded,\nand a recompute afterwards can only rebuild what the underlying readings still cover.\n\nTwo interlocks, and only one of them is waivable. The derivation must ALREADY be disabled —\n--force does not waive that, because disabling is one reversible command and it makes you\nwatch the thing stop first. Then anything still relying on it (its intervals, an hws model's\noutput point, an automation whose trigger names it) refuses the delete and NAMES what would\nbreak; --force is your answer to that list, and the result reports what it overrode.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the delete (the reason names what).\n\nExamples:\n  liveone derivation delete dx_01k9y6vdqefyz8p4wsy5j5hgcx\n  liveone derivation delete dx_01k9y6vdqefyz8p4wsy5j5hgcx --apply\n  liveone derivation delete dx_01k9y6vdqefyz8p4wsy5j5hgcx --force --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "derivation": {
            "type": "string",
            "description": "A derivation: its dx_… id, its name, or its role (narrow with --device= if a role names more than one)"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "device": {
            "type": "string",
            "description": "Only derivations touching this device (dv_… id, handle, slug or name) (ref)"
          },
          "area": {
            "type": "string",
            "description": "Only derivations touching one of this area's member devices (ref)"
          },
          "force": {
            "type": "boolean",
            "description": "Proceed even though something still relies on it (does NOT waive the disabled-first interlock)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "derivation"
        ]
      },
      "signature": "liveone derivation delete <derivation> { --base-url?: string; --device?: string; --area?: string; --force?: boolean }",
      "invocation": "npm run liveone -- derivation delete",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__derivation__disable",
      "summary": "Stop a derivation being recomputed. Its existing rows are untouched.",
      "when": "This is the safe lever, and the reversible one: a disabled derivation stops being\nrecomputed and stops advertising its capability, while its history stays exactly as it was.\nIt is also the required first step of a `delete` — deliberately, so the thing is watched\nstopping before it is destroyed.",
      "parentWhen": "Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.",
      "description": "npm run liveone -- derivation disable — Stop a derivation being recomputed. Its existing rows are untouched.\n\nThis is the safe lever, and the reversible one: a disabled derivation stops being\nrecomputed and stops advertising its capability, while its history stays exactly as it was.\nIt is also the required first step of a `delete` — deliberately, so the thing is watched\nstopping before it is destroyed.\n\nAbout `derivation` generally: Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone derivation disable ev --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "derivation": {
            "type": "string",
            "description": "A derivation: its dx_… id, its name, or its role (narrow with --device= if a role names more than one)"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "device": {
            "type": "string",
            "description": "Only derivations touching this device (dv_… id, handle, slug or name) (ref)"
          },
          "area": {
            "type": "string",
            "description": "Only derivations touching one of this area's member devices (ref)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "derivation"
        ]
      },
      "signature": "liveone derivation disable <derivation> { --base-url?: string; --device?: string; --area?: string }",
      "invocation": "npm run liveone -- derivation disable",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__derivation__enable",
      "summary": "Re-enable a derivation, so it is recomputed again.",
      "when": "Use this after a `disable`, once whatever was wrong with its inputs is fixed.",
      "parentWhen": "Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.",
      "description": "npm run liveone -- derivation enable — Re-enable a derivation, so it is recomputed again.\n\nUse this after a `disable`, once whatever was wrong with its inputs is fixed.\n\nAbout `derivation` generally: Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone derivation enable ev --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "derivation": {
            "type": "string",
            "description": "A derivation: its dx_… id, its name, or its role (narrow with --device= if a role names more than one)"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "device": {
            "type": "string",
            "description": "Only derivations touching this device (dv_… id, handle, slug or name) (ref)"
          },
          "area": {
            "type": "string",
            "description": "Only derivations touching one of this area's member devices (ref)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "derivation"
        ]
      },
      "signature": "liveone derivation enable <derivation> { --base-url?: string; --device?: string; --area?: string }",
      "invocation": "npm run liveone -- derivation enable",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__derivation__intervals",
      "summary": "The rows a derivation has produced — runs, newest first.",
      "when": "Use this to check what a detector actually found: after a create, after a backfill, or when\na dashboard's runs card looks wrong and you want the numbers behind it.",
      "parentWhen": "Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.",
      "description": "npm run liveone -- derivation intervals — The rows a derivation has produced — runs, newest first.\n\nUse this to check what a detector actually found: after a create, after a backfill, or when\na dashboard's runs card looks wrong and you want the numbers behind it.\n\nAbout `derivation` generally: Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.\n\nRaw values, not the display strings the dashboard card's own endpoint serves — ISO instants\nand numbers, with the unit each signal statistic is in carried PER ROW (a window can\nstraddle a detector re-point and hold two units).\nA run belongs to a window if it STARTED in it, which is the same rule `recompute` deletes\nby — so these are exactly the rows a recompute over the same window would replace.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = no intervals in the window.\n\nExamples:\n  liveone derivation intervals ev --device=kutis --last=60d\n  liveone derivation intervals ev --last=7d --format json",
      "input_schema": {
        "type": "object",
        "properties": {
          "derivation": {
            "type": "string",
            "description": "A derivation: its dx_… id, its name, or its role (narrow with --device= if a role names more than one)"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "device": {
            "type": "string",
            "description": "Only derivations touching this device (dv_… id, handle, slug or name) (ref)"
          },
          "area": {
            "type": "string",
            "description": "Only derivations touching one of this area's member devices (ref)"
          },
          "last": {
            "type": "string",
            "description": "Relative window ending now (30d)"
          },
          "date": {
            "type": "string",
            "description": "A single UTC day (YYYY-MM-DD)"
          },
          "start": {
            "type": "string",
            "description": "Window start (UTC) (YYYY-MM-DD)"
          },
          "end": {
            "type": "string",
            "description": "Window end, inclusive (UTC) (YYYY-MM-DD)"
          },
          "limit": {
            "type": "number",
            "description": "Rows to return, max 500 (default 100) (n)"
          },
          "offset": {
            "type": "number",
            "description": "Skip this many (n)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "derivation"
        ]
      },
      "signature": "liveone derivation intervals <derivation> { --base-url?: string; --device?: string; --area?: string; --last?: string; --date?: string; --start?: string; --end …",
      "invocation": "npm run liveone -- derivation intervals",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__derivation__list",
      "summary": "The derivations you can read: id, kind, role, enabled, devices, sources.",
      "when": "Start here — to find a dx_… id, or to check whether a detector exists at all.",
      "parentWhen": "Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.",
      "description": "npm run liveone -- derivation list — The derivations you can read: id, kind, role, enabled, devices, sources.\n\nStart here — to find a dx_… id, or to check whether a detector exists at all.\n\nAbout `derivation` generally: Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.\n\nFleet-wide by default: the collection returns every derivation you can read, so 'which\ndetectors exist anywhere' is one call. Narrow it with a positional scope (resolved as a\nDEVICE first, then as an area — every device has an area of one with the same name) or\nwith the explicit --device/--area flags. Access is all-or-nothing per derivation: one\nwhose device set you cannot fully read is absent rather than partially shown.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = nothing matched.\n\nExamples:\n  liveone derivation list\n  liveone derivation list daylesford\n  liveone derivation list --role=generator --disabled\n  liveone derivation list --format json",
      "input_schema": {
        "type": "object",
        "properties": {
          "scope": {
            "type": "string",
            "description": "Optional: only derivations touching this DEVICE (or, failing that, this area). Bare = everything you can read"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "device": {
            "type": "string",
            "description": "Only derivations touching this device (dv_… id, handle, slug or name) (ref)"
          },
          "area": {
            "type": "string",
            "description": "Only derivations touching one of this area's member devices (ref)"
          },
          "kind": {
            "type": "string",
            "description": "Only this kind of derivation",
            "enum": [
              "run-detector",
              "hws-model"
            ]
          },
          "role": {
            "type": "string",
            "description": "Only detectors for this role",
            "enum": [
              "generator",
              "ev"
            ]
          },
          "enabled": {
            "type": "boolean",
            "description": "Only enabled derivations"
          },
          "disabled": {
            "type": "boolean",
            "description": "Only disabled derivations"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone derivation list [scope] { --base-url?: string; --device?: string; --area?: string; --kind?: \"run-detector\"|\"hws-model\"; --role?: \"generator\"|\"ev\"; --e …",
      "invocation": "npm run liveone -- derivation list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__derivation__recompute",
      "summary": "Rebuild ONE derivation's intervals over a window.",
      "when": "Use this to backfill history for a detector you just created, or to rebuild after changing\nits params. The minutely cron already heals the trailing 6 hours, so this is for anything\nolder than that.",
      "parentWhen": "Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.",
      "description": "npm run liveone -- derivation recompute — Rebuild ONE derivation's intervals over a window.\n\nUse this to backfill history for a detector you just created, or to rebuild after changing\nits params. The minutely cron already heals the trailing 6 hours, so this is for anything\nolder than that.\n\nAbout `derivation` generally: Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.\n\n🛑 regenerate and delete are DELETE-AND-REINSERT. That is safe here only because the\nderivation is named in the request path, so this verb has no unscoped form — unlike the\ncron's twin, whose filter is optional and through which a full-range unscoped regenerate\nonce collapsed 71 rows to 3.\n\nNo window means ALL of this detector's history. A long span may exceed the route's 300s\nbudget; run it in --last=30d slices if so (the server chunks at 14 days internally and\nretries transient database errors, so a re-run is cheap and safe to repeat).\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone derivation recompute ev --device=kutis --start=2026-07-06 --end=2026-09-01\n  liveone derivation recompute ev --last=30d --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "derivation": {
            "type": "string",
            "description": "A derivation: its dx_… id, its name, or its role (narrow with --device= if a role names more than one)"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "device": {
            "type": "string",
            "description": "Only derivations touching this device (dv_… id, handle, slug or name) (ref)"
          },
          "area": {
            "type": "string",
            "description": "Only derivations touching one of this area's member devices (ref)"
          },
          "last": {
            "type": "string",
            "description": "Relative window ending now (30d)"
          },
          "date": {
            "type": "string",
            "description": "A single UTC day (YYYY-MM-DD)"
          },
          "start": {
            "type": "string",
            "description": "Window start (UTC) (YYYY-MM-DD)"
          },
          "end": {
            "type": "string",
            "description": "Window end, inclusive (UTC) (YYYY-MM-DD)"
          },
          "action": {
            "type": "string",
            "description": "regenerate = purge then rebuild; aggregate = rebuild in place; delete = purge only",
            "enum": [
              "regenerate",
              "aggregate",
              "delete"
            ],
            "default": "regenerate"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "derivation"
        ]
      },
      "signature": "liveone derivation recompute <derivation> { --base-url?: string; --device?: string; --area?: string; --last?: string; --date?: string; --start?: string; --end …",
      "invocation": "npm run liveone -- derivation recompute",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__derivation__set",
      "summary": "Change a derivation's threshold params, its boundary point, or rename it.",
      "when": "Use this when a detector is firing wrongly — most often when it FRAGMENTS one long run into\nmany short ones, which means --delay-off is at or below the point's sample interval.",
      "parentWhen": "Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.",
      "description": "npm run liveone -- derivation set — Change a derivation's threshold params, its boundary point, or rename it.\n\nUse this when a detector is firing wrongly — most often when it FRAGMENTS one long run into\nmany short ones, which means --delay-off is at or below the point's sample interval.\n\nAbout `derivation` generally: Reach for this when a device's RUNS are the question — whether the generator/EV-charge detector\nexists, what it has detected, or rebuilding its history after a config change. The `runs` card on\na dashboard shows what a detector here produced; `area` and `device` show the inputs it reads.\n\nParams are MERGED into what is stored, then sent as a whole object (the API replaces\n`params` wholesale, and a blind replace would silently drop the knobs you did not mention).\nUse --unset to remove a pinned knob so it goes back to inheriting the role default.\n\n🛑 This does NOT rewrite history. Existing intervals were detected under the OLD params and\nstay exactly as they were until you `recompute` the window you care about.\n\nThe signal and energy points are deliberately not editable here: re-pointing a detector\nchanges what its already-stored rows MEAN, and those rows carry the old signal's unit with\nno way to know they predate the change. That is a considered manual operation, not a flag.\n--boundary is the ONE exception, and it is safe in exactly the way the others are not: the\nboundary point does not change what the stored numbers measure, only where two adjacent\nruns are divided. It resolves against the devices this derivation already touches; a\nqualified `<device>:<path>` reaches elsewhere and WIDENS the device set, so write access is\nthen required on that device too.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = nothing to change.\n\nExamples:\n  liveone derivation set ev --delay-off=900\n  liveone derivation set ev --delay-off=900 --apply\n  liveone derivation set generator --device=daylesford --unset=hysteresis --apply\n  liveone derivation set dx_01k9y6vdqefyz8p4wsy5j5hgcx --boundary=source.generator.control.request/duration --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "derivation": {
            "type": "string",
            "description": "A derivation: its dx_… id, its name, or its role (narrow with --device= if a role names more than one)"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "device": {
            "type": "string",
            "description": "Only derivations touching this device (dv_… id, handle, slug or name) (ref)"
          },
          "area": {
            "type": "string",
            "description": "Only derivations touching one of this area's member devices (ref)"
          },
          "upper": {
            "type": "number",
            "description": "Above this, the device is ON (W)"
          },
          "lower": {
            "type": "number",
            "description": "Below this, the device is OFF (W)"
          },
          "hysteresis": {
            "type": "number",
            "description": "Deadband around the threshold (W)"
          },
          "delay-on": {
            "type": "number",
            "description": "Ignore an on-signal shorter than this (seconds)"
          },
          "delay-off": {
            "type": "number",
            "description": "Bridge a gap shorter than this. 🛑 Must comfortably EXCEED the point's sample interval, or every poll gap closes a run (seconds)"
          },
          "name": {
            "type": "string",
            "description": "Rename it (text)"
          },
          "boundary": {
            "type": "string",
            "description": "run-detector only: the control point whose edges cut runs apart (path|pt_)"
          },
          "clear-boundary": {
            "type": "boolean",
            "description": "Remove the boundary point, so nothing forces a run to end"
          },
          "unset": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Drop a pinned knob, so it inherits the role default again (knob)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "derivation"
        ]
      },
      "signature": "liveone derivation set <derivation> { --base-url?: string; --device?: string; --area?: string; --upper?: number; --lower?: number; --hysteresis?: number; --de …",
      "invocation": "npm run liveone -- derivation set",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__device__history",
      "summary": "Time series for a device, in the OpenNEM shape /api/history serves.",
      "when": "Use this to pull a device's measured series over a window. The human rendering is a\nper-series summary; the full payload goes to --out (or --format json).",
      "parentWhen": "Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.",
      "description": "npm run liveone -- device history — Time series for a device, in the OpenNEM shape /api/history serves.\n\nUse this to pull a device's measured series over a window. The human rendering is a\nper-series summary; the full payload goes to --out (or --format json).\n\nAbout `device` generally: Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.\n\n--start/--end are whole LOCAL days (the device's fixed day offset — the same boundaries\nthe daily aggregates roll up on). One request regardless of span; bound long sub-daily\npulls with --series. Shapes: --format json nests the full OpenNEM body under `response`;\n--out writes the RAW body; each series carries\nhistory.{firstInterval,lastInterval,interval,numIntervals,data}.\n--format csv emits WIDE rows — timestamp_local, timestamp_utc, then one column per\nseries with the unit in the header (`13/load/power.avg (W)`); nulls are empty cells.\nWith --out the CSV goes to the file and stdout gets the summary (as JSON).\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = no series matched (the window, or the --list-series subject).\n\nExamples:\n  liveone device history daylesford --list-series\n  liveone device history daylesford --last=3h\n  liveone device history daylesford --last=7d --series=\"load/*\" --format=csv --out=load.csv\n  liveone device history daylesford --interval=1d --start=2026-07-01 --end=2026-07-31",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "A device: its dv_… id, integer handle, slug, or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "interval": {
            "type": "string",
            "description": "Series resolution (range caps per request: 5m ≤ 31 days, 30m/1d ≤ 13 months)",
            "enum": [
              "5m",
              "30m",
              "1d"
            ],
            "default": "5m"
          },
          "last": {
            "type": "string",
            "description": "Relative window ending now, e.g. 3h, 7d (default: 1d; the server owns the grammar) (7d)"
          },
          "start": {
            "type": "string",
            "description": "Window start — whole LOCAL days (the subject's fixed day offset) (YYYY-MM-DD)"
          },
          "end": {
            "type": "string",
            "description": "Window end, inclusive (local days) (YYYY-MM-DD)"
          },
          "series": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Only series matching this glob, matched against the DEVICE-LESS path, e.g. \"load/*\", \"**/energy.delta\" (repeatable; `*` does not cross `/`) (glob)"
          },
          "out": {
            "type": "string",
            "description": "Write the raw OpenNEM body (or the CSV, under --format csv) to this file; stdout gets a summary (path)"
          },
          "list-series": {
            "type": "boolean",
            "description": "List series METADATA only — id, unit, metric type, stat suffix, declared intervals, data extents and sample count; no data arrays. The natural first call against an unfamiliar subject. Refuses time flags; --interval is ignored (the per-series `intervals` field answers it)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json",
              "csv"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "device"
        ]
      },
      "signature": "liveone device history <device> { --base-url?: string; --interval?: \"5m\"|\"30m\"|\"1d\"; --last?: string; --start?: string; --end?: string; --series?: string[]; - …",
      "invocation": "npm run liveone -- device history",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__device__latest",
      "summary": "The device's current values, from the serving cache.",
      "when": "Use this for 'what is it doing NOW' — the same latest map every dashboard card reads.\nFor anything with a time axis use `history`.",
      "parentWhen": "Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.",
      "description": "npm run liveone -- device latest — The device's current values, from the serving cache.\n\nUse this for 'what is it doing NOW' — the same latest map every dashboard card reads.\nFor anything with a time axis use `history`.\n\nAbout `device` generally: Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone device latest daylesford",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "A device: its dv_… id, integer handle, slug, or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "device"
        ]
      },
      "signature": "liveone device latest <device> { --base-url?: string }",
      "invocation": "npm run liveone -- device latest",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__device__list",
      "summary": "List the devices you can read: id, handle, vendor, status, name.",
      "when": "Start here when you do not yet know a device's id.",
      "parentWhen": "Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.",
      "description": "npm run liveone -- device list — List the devices you can read: id, handle, vendor, status, name.\n\nStart here when you do not yet know a device's id.\n\nAbout `device` generally: Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone device list\n  liveone device list --vendor=amber",
      "input_schema": {
        "type": "object",
        "properties": {
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "vendor": {
            "type": "string",
            "description": "Only this vendor's devices (vendor)"
          },
          "status": {
            "type": "string",
            "description": "Only devices with this status (active, disabled, removed) (status)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone device list { --base-url?: string; --vendor?: string; --status?: string }",
      "invocation": "npm run liveone -- device list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__device__points",
      "summary": "A device's point inventory: pt_… id, path, metric, unit.",
      "when": "Use this to find a point's id or path — e.g. before wiring a binding or reading a\nspecific series.",
      "parentWhen": "Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.",
      "description": "npm run liveone -- device points — A device's point inventory: pt_… id, path, metric, unit.\n\nUse this to find a point's id or path — e.g. before wiring a binding or reading a\nspecific series.\n\nAbout `device` generally: Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone device points daylesford",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "A device: its dv_… id, integer handle, slug, or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "device"
        ]
      },
      "signature": "liveone device points <device> { --base-url?: string }",
      "invocation": "npm run liveone -- device points",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__device__recompute",
      "summary": "Rebuild the rows derived FROM a device's readings, over a window of local days.",
      "when": "Run this AFTER a `liveone sync` (or any other repair) has landed, for the same window.\nDerived rows are pure functions of their sources and nothing rebuilds a past day on its\nown, so a backfill without this leaves the dashboards showing the hole it just filled.\nFor RUN DETECTORS — generator runs, EV charge sessions — use `derivation recompute`\ninstead; they are derivations and are rebuilt by their own scoped verb.",
      "parentWhen": "Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.",
      "description": "npm run liveone -- device recompute — Rebuild the rows derived FROM a device's readings, over a window of local days.\n\nRun this AFTER a `liveone sync` (or any other repair) has landed, for the same window.\nDerived rows are pure functions of their sources and nothing rebuilds a past day on its\nown, so a backfill without this leaves the dashboards showing the hole it just filled.\nFor RUN DETECTORS — generator runs, EV charge sessions — use `derivation recompute`\ninstead; they are derivations and are rebuilt by their own scoped verb.\n\nAbout `device` generally: Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.\n\nRebuilds `agg_1d` for each day, then the attributed flow matrix of every Area the\ndevice's points bind into, re-folding the battery blend first where the Area has one.\n\nSCOPED, deliberately: it does not run the fleet-wide HWS, battery-learning and backlog\nreheal passes that `/api/cron/daily` does. Those exist to find days that went stale for\nreasons unconnected to this repair, and sweeping the fleet's backlog is the nightly\nsweep's job — measured on prod, a one-day backfill spent an entire 300s budget in it.\n\n🛑 The window is REQUIRED and capped at 31 days. There is no unscoped form and no\n'absent means everything': the fleet-wide twin reads a missing date as ALL HISTORY, and a\nverb whose dangerous case is the one you get by typing less will eventually be typed\nless. Days are the DEVICE's local days — the boundaries its daily aggregates roll up on.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone device recompute kutis --date=2026-09-10\n  liveone device recompute 13 --start=2026-09-10 --end=2026-09-11 --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "A device: its dv_… id, integer handle, slug, or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "date": {
            "type": "string",
            "description": "A single local day (YYYY-MM-DD)"
          },
          "start": {
            "type": "string",
            "description": "Window start (local days) (YYYY-MM-DD)"
          },
          "end": {
            "type": "string",
            "description": "Window end, inclusive (local days) (YYYY-MM-DD)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "device"
        ]
      },
      "signature": "liveone device recompute <device> { --base-url?: string; --date?: string; --start?: string; --end?: string }",
      "invocation": "npm run liveone -- device recompute",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__device__show",
      "summary": "A device's full aggregate: metadata, config, adapter state, capabilities, points.",
      "when": "Use this to see everything the platform knows about one device — its vendor identity,\nconfig overrides, derived capabilities and point inventory.",
      "parentWhen": "Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.",
      "description": "npm run liveone -- device show — A device's full aggregate: metadata, config, adapter state, capabilities, points.\n\nUse this to see everything the platform knows about one device — its vendor identity,\nconfig overrides, derived capabilities and point inventory.\n\nAbout `device` generally: Reach for this for the PHYSICAL/vendor layer: what a device IS and what it reports. For the\nsemantic grouping (areas, bindings, flows) use `area`; for what a dashboard shows use\n`dashboard`.\n\nThe aggregate is an OBJECT, so the human rendering is the pretty-printed JSON — a table\nwould only hide its shape. `points` renders the point inventory alone, as a table.\n`capabilities` are DERIVED (a point scan + compound predicates), and `area show` remains\nthe authoritative place to read them in context — its members carry the same list.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone device show daylesford\n  liveone device show dv_01kybrhzkmfyxvz63d15rscj19",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "A device: its dv_… id, integer handle, slug, or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "device"
        ]
      },
      "signature": "liveone device show <device> { --base-url?: string }",
      "invocation": "npm run liveone -- device show",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__find",
      "summary": "Find the command for a job, in plain English.",
      "when": "Reach for this FIRST when you know what you want to do but not which command does it —\ninstead of reading CLI_README.md end to end, or grepping the generated prose. Offline and\ninstant: it ranks the committed catalogue, and imports nothing.",
      "parentWhen": "The entry point for operating LiveOne from a terminal or an agent. Pick the domain that owns\nthe thing you want to read or change — `dashboard` for what a dashboard SHOWS — then the verb.\nRun `liveone <domain> --help` to see a domain's verbs.",
      "description": "npm run liveone -- find — Find the command for a job, in plain English.\n\nReach for this FIRST when you know what you want to do but not which command does it —\ninstead of reading CLI_README.md end to end, or grepping the generated prose. Offline and\ninstant: it ranks the committed catalogue, and imports nothing.\n\nAbout `liveone` generally: The entry point for operating LiveOne from a terminal or an agent. Pick the domain that owns\nthe thing you want to read or change — `dashboard` for what a dashboard SHOWS — then the verb.\nRun `liveone <domain> --help` to see a domain's verbs.\n\nPrints the best few matches with a one-line signature each — enough to construct the call.\nUse --show <name> for one command's full argument schema, and --index for the whole\nname+summary spine. Results are trimmed to a character budget, and `truncated` is always\nreported rather than a short list quietly looking complete.\n\nRead-only. This command changes nothing.\n\nExit codes: 0, 1, 2, 130. For this command, 1 = nothing matched.\n\nExamples:\n  liveone find \"edit a dashboard card\"\n  liveone find \"log in\" --limit=3\n  liveone find --show liveone__dashboard__set-prop\n  liveone find --index",
      "input_schema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "What you are trying to do, in words"
          },
          "show": {
            "type": "string",
            "description": "Print one command's full catalogue entry, including its input schema (name)"
          },
          "index": {
            "type": "boolean",
            "description": "List every command — name and summary only"
          },
          "limit": {
            "type": "number",
            "description": "How many matches to return — a positive integer",
            "default": 5
          },
          "budget": {
            "type": "number",
            "description": "Character budget for the results — a positive integer",
            "default": 4000
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone find [query...] { --show?: string; --index?: boolean; --limit?: number; --budget?: number }",
      "invocation": "npm run liveone -- find",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        130
      ]
    },
    {
      "name": "liveone__owner__show",
      "summary": "Who owns a device, an area or a dashboard.",
      "when": "Ownership is what carries data access, so this is the first question before any transfer.\nGive any ref — the verb works out which kind of thing it is.",
      "parentWhen": "Reach for this when something changes hands. For what an area is MADE of use `area`; for\nwhat a dashboard shows use `dashboard`.",
      "description": "npm run liveone -- owner show — Who owns a device, an area or a dashboard.\n\nOwnership is what carries data access, so this is the first question before any transfer.\nGive any ref — the verb works out which kind of thing it is.\n\nAbout `owner` generally: Reach for this when something changes hands. For what an area is MADE of use `area`; for\nwhat a dashboard shows use `dashboard`.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = nothing of any kind matched that ref.\n\nExamples:\n  liveone owner show kutis\n  liveone owner show 13",
      "input_schema": {
        "type": "object",
        "properties": {
          "thing": {
            "type": "string",
            "description": "A device, area or dashboard: its TypeID, handle, slug or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "thing"
        ]
      },
      "signature": "liveone owner show <thing> { --base-url?: string }",
      "invocation": "npm run liveone -- owner show",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__owner__transfer",
      "summary": "Move devices/areas/dashboards to a new owner, and share them back — one transaction.",
      "when": "Reach for this when a site changes hands. 🛑 Read access is DERIVED from ownership plus\ndashboard grants, so the moment ownership moves the outgoing owner loses access — and\ngranting is itself an owner-side action. Transfer and share-back therefore happen in ONE\nserver-side transaction: either both, or neither.",
      "parentWhen": "Reach for this when something changes hands. For what an area is MADE of use `area`; for\nwhat a dashboard shows use `dashboard`.",
      "description": "npm run liveone -- owner transfer — Move devices/areas/dashboards to a new owner, and share them back — one transaction.\n\nReach for this when a site changes hands. 🛑 Read access is DERIVED from ownership plus\ndashboard grants, so the moment ownership moves the outgoing owner loses access — and\ngranting is itself an owner-side action. Transfer and share-back therefore happen in ONE\nserver-side transaction: either both, or neither.\n\nAbout `owner` generally: Reach for this when something changes hands. For what an area is MADE of use `area`; for\nwhat a dashboard shows use `dashboard`.\n\nThe share-back is checked, not assumed: the server REFUSES a transfer after which a\nshare-back recipient could not read a transferred device, and names the devices. That is\nwhat catches the common mistake of moving devices without the dashboard that shows them.\n\n`--cascade` expands an area to its member DEVICES only. Dashboards are never swept in\nimplicitly — they are what grants are written against, so moving one silently would change\nwho can see what. Any dashboard referencing a moving device is listed in the plan instead.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = the server refused the transfer (the reason says why).\n\nExamples:\n  liveone owner transfer karoline@example.com --areas=kutis --cascade --dashboards=kew,kew-stacked\n  liveone owner transfer karoline@example.com --areas=kutis --cascade --dashboards=kew --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "to": {
            "type": "string",
            "description": "The new owner: user_… id, email, or username"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "devices": {
            "type": "string",
            "description": "Devices to move (comma-separated refs) (a,b)"
          },
          "areas": {
            "type": "string",
            "description": "Areas to move (comma-separated refs) (a,b)"
          },
          "dashboards": {
            "type": "string",
            "description": "Dashboards to move (comma-separated refs) (a,b)"
          },
          "cascade": {
            "type": "boolean",
            "description": "Also move each named area's member devices"
          },
          "share-back": {
            "type": "string",
            "description": "Users to grant on the moved dashboards (comma-separated); defaults to you (user)"
          },
          "no-share-back": {
            "type": "boolean",
            "description": "Transfer with NO share-back — only the new owner and admins will see it"
          },
          "role": {
            "type": "string",
            "description": "Grant role for the share-back (default: viewer) (viewer)",
            "enum": [
              "viewer",
              "admin"
            ]
          },
          "force": {
            "type": "boolean",
            "description": "Proceed even if the share-back would not restore read access"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "to"
        ]
      },
      "signature": "liveone owner transfer <to> { --base-url?: string; --devices?: string; --areas?: string; --dashboards?: string; --cascade?: boolean; --share-back?: string; -- …",
      "invocation": "npm run liveone -- owner transfer",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__queue__outbox",
      "summary": "Why is publishing failing? The durable buffer's backlog, and the error the relay recorded.",
      "when": "Reach for this when `status` says ingest has stalled but BOTH transports read empty —\nnothing waiting, nothing in flight. That is what a broken PUBLISH looks like: the message\nnever reached QStash at all, so no QStash view can explain it. The reason is in Postgres.",
      "parentWhen": "Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.",
      "description": "npm run liveone -- queue outbox — Why is publishing failing? The durable buffer's backlog, and the error the relay recorded.\n\nReach for this when `status` says ingest has stalled but BOTH transports read empty —\nnothing waiting, nothing in flight. That is what a broken PUBLISH looks like: the message\nnever reached QStash at all, so no QStash view can explain it. The reason is in Postgres.\n\nAbout `queue` generally: Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.\n\n🛑 Read `failing`, not `backlog`. An unpublished row means the relay has not got to it\nyet, which is the normal steady state between minutes. `attempts > 0` with a `lastError`\nis the difference between an ingest path that is behind and one that is broken.\n\nExits 1 (findings) when anything is failing, so it composes into a check.\n\nNothing here is lost: the outbox is teed BEFORE publishing and retains payloads for 30\ndays, so a failing publish is a latency problem that the relay clears once it can send.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone queue outbox\n  liveone queue outbox --limit=50",
      "input_schema": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "string",
            "description": "How many failing rows to show, 1..200 (default: 20). One reason repeated is one finding. (n)"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone queue outbox { --limit?: string; --base-url?: string }",
      "invocation": "npm run liveone -- queue outbox",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__queue__parallelism",
      "summary": "Read, or PIN, how many messages a lane delivers concurrently. The SUM across lanes is capped by the PG pool.",
      "when": "Raise this when one slow message is head-of-line blocking a lane. With no argument it\nreads every lane and writes nothing. Setting a value requires `--lane`.",
      "parentWhen": "Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.",
      "description": "npm run liveone -- queue parallelism — Read, or PIN, how many messages a lane delivers concurrently. The SUM across lanes is capped by the PG pool.\n\nRaise this when one slow message is head-of-line blocking a lane. With no argument it\nreads every lane and writes nothing. Setting a value requires `--lane`.\n\nAbout `queue` generally: Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.\n\nSetting PINS the cap. Our own publishes carry a parallelism, so an unpinned change is\nreverted by the next published message within ~60s — pinning is what makes it stick.\n`--lane=<lane> 0` unpins, handing the lane back to the publish-time default.\n\nThe ceiling is on the SUM across lanes, not on either lane alone: every in-flight\ndelivery holds a Postgres connection, so starving the web app to drain a backlog trades\none outage for another. The server owns that check and names the sum when it refuses.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone queue parallelism\n  liveone queue parallelism 5 --lane=live --apply\n  liveone queue parallelism 0 --lane=backfill --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "n": {
            "type": "string",
            "description": "New concurrency for --lane, or 0 to unpin. Omit to read every lane."
          },
          "lane": {
            "type": "string",
            "description": "Which lane: live, backfill, or all. Required to SET parallelism (a per-lane cap is never applied fleet-wide). (lane)",
            "enum": [
              "live",
              "backfill",
              "all"
            ]
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        }
      },
      "signature": "liveone queue parallelism [n] { --lane?: \"live\"|\"backfill\"|\"all\"; --base-url?: string }",
      "invocation": "npm run liveone -- queue parallelism",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__queue__pause",
      "summary": "Stop a lane dispatching. Messages accumulate; nothing is lost.",
      "when": "Use this to stop delivery while you diagnose, or before a change that would make the\nreceiver fail. Publishing is unaffected — the outbox keeps accepting.\nPausing `backfill` alone is how you protect live ingest from a bulk import.",
      "parentWhen": "Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.",
      "description": "npm run liveone -- queue pause — Stop a lane dispatching. Messages accumulate; nothing is lost.\n\nUse this to stop delivery while you diagnose, or before a change that would make the\nreceiver fail. Publishing is unaffected — the outbox keeps accepting.\nPausing `backfill` alone is how you protect live ingest from a bulk import.\n\nAbout `queue` generally: Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone queue pause --apply\n  liveone queue pause --lane=backfill --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "lane": {
            "type": "string",
            "description": "Which lane: live, backfill, or all. Required to SET parallelism (a per-lane cap is never applied fleet-wide). (lane)",
            "enum": [
              "live",
              "backfill",
              "all"
            ]
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        }
      },
      "signature": "liveone queue pause { --lane?: \"live\"|\"backfill\"|\"all\"; --base-url?: string }",
      "invocation": "npm run liveone -- queue pause",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__queue__resume",
      "summary": "Resume dispatching after a pause.",
      "when": "The inverse of `pause`. Defaults to every lane.",
      "parentWhen": "Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.",
      "description": "npm run liveone -- queue resume — Resume dispatching after a pause.\n\nThe inverse of `pause`. Defaults to every lane.\n\nAbout `queue` generally: Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone queue resume --apply\n  liveone queue resume --lane=backfill --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "lane": {
            "type": "string",
            "description": "Which lane: live, backfill, or all. Required to SET parallelism (a per-lane cap is never applied fleet-wide). (lane)",
            "enum": [
              "live",
              "backfill",
              "all"
            ]
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        }
      },
      "signature": "liveone queue resume { --lane?: \"live\"|\"backfill\"|\"all\"; --base-url?: string }",
      "invocation": "npm run liveone -- queue resume",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__queue__status",
      "summary": "Is ingest flowing? Per-lane waiting / in-flight / parallelism, and minutes since the last durable write.",
      "when": "Start here. Exits 1 (findings) when ingest has stalled or any lane is STUCK, so it\ncomposes into a check. `--lane` narrows the table; the verdict still spans the path.",
      "parentWhen": "Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.",
      "description": "npm run liveone -- queue status — Is ingest flowing? Per-lane waiting / in-flight / parallelism, and minutes since the last durable write.\n\nStart here. Exits 1 (findings) when ingest has stalled or any lane is STUCK, so it\ncomposes into a check. `--lane` narrows the table; the verdict still spans the path.\n\nAbout `queue` generally: Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone queue status\n  liveone queue status --lane=backfill",
      "input_schema": {
        "type": "object",
        "properties": {
          "lane": {
            "type": "string",
            "description": "Only this lane: live, backfill, or all (default: all). (lane)",
            "enum": [
              "live",
              "backfill",
              "all"
            ]
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone queue status { --lane?: \"live\"|\"backfill\"|\"all\"; --base-url?: string }",
      "invocation": "npm run liveone -- queue status",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__queue__timing",
      "summary": "How long each batch actually took — per-message wait, duration, attempts and outcome.",
      "when": "Reach for this after `status` says something is wrong, to find out WHICH kind of wrong.\nRead `wait` and `duration` as a pair: a long wait with a short duration means something\nAHEAD of that batch held the delivery slot (head-of-line); a long duration is the batch's\nown work. `lag` cannot tell those apart, and conflating them misdiagnosed 2026-09-09 twice.\nAlso the way to watch a large backfill: durations should stay flat as it runs.",
      "parentWhen": "Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.",
      "description": "npm run liveone -- queue timing — How long each batch actually took — per-message wait, duration, attempts and outcome.\n\nReach for this after `status` says something is wrong, to find out WHICH kind of wrong.\nRead `wait` and `duration` as a pair: a long wait with a short duration means something\nAHEAD of that batch held the delivery slot (head-of-line); a long duration is the batch's\nown work. `lag` cannot tell those apart, and conflating them misdiagnosed 2026-09-09 twice.\nAlso the way to watch a large backfill: durations should stay flat as it runs.\n\nAbout `queue` generally: Reach for this when readings have stopped arriving, or before and after a large backfill.\n`status` is the one-screen health read; `parallelism` is the lever that clears a stall.\n\nA windowed forensic read, paged out of QStash's delivery log, so keep the window tight —\n`--last` defaults to 1h.\n\n🛑 The retention is QStash's, not ours: old windows simply return nothing, which is NOT\nthe same as a quiet window. `truncated` marks a window that outran the page budget, and\nevery count under it is an undercount.\n\nA batch that fails FAST still occupies its slot for the whole retry schedule, so watch\n`retries` and `occupancy` alongside `duration` — a wedged FIFO queue looks the same either\nway from the outside.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone queue timing\n  liveone queue timing --last=6h --failed\n  liveone queue timing --lane=backfill --last=30m\n  liveone queue timing --from=2026-09-09T10:30:00Z --to=2026-09-09T13:00:00Z",
      "input_schema": {
        "type": "object",
        "properties": {
          "last": {
            "type": "string",
            "description": "Window ending now: 90s / 15m / 2h / 7d (default: 1h). Mutually exclusive with --from. (2h)"
          },
          "from": {
            "type": "string",
            "description": "Window start — an ISO instant or epoch-ms. Use with --to for a fixed window. (when)"
          },
          "to": {
            "type": "string",
            "description": "Window end (default: now). Only with --from. (when)"
          },
          "failed": {
            "type": "boolean",
            "description": "Only batches that did not deliver — the ones worth reading first."
          },
          "limit": {
            "type": "string",
            "description": "Show at most this many batches (default: 20). The summary always spans them all. (n)"
          },
          "lane": {
            "type": "string",
            "description": "Only this lane: live, backfill, or all (default: all). (lane)",
            "enum": [
              "live",
              "backfill",
              "all"
            ]
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone queue timing { --last?: string; --from?: string; --to?: string; --failed?: boolean; --limit?: string; --lane?: \"live\"|\"backfill\"|\"all\"; --base-url?: s …",
      "invocation": "npm run liveone -- queue timing",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__sync",
      "summary": "Re-fetch a historical window from a device's vendor, on the backfill lane.",
      "when": "Reach for this when a device is MISSING history a vendor still holds — a gap found by\n`liveone device history --list-series`, or a device connected after the fact. For what the\ningest path is doing while it runs, use `liveone queue`.",
      "parentWhen": "The entry point for operating LiveOne from a terminal or an agent. Pick the domain that owns\nthe thing you want to read or change — `dashboard` for what a dashboard SHOWS — then the verb.\nRun `liveone <domain> --help` to see a domain's verbs.",
      "description": "npm run liveone -- sync — Re-fetch a historical window from a device's vendor, on the backfill lane.\n\nReach for this when a device is MISSING history a vendor still holds — a gap found by\n`liveone device history --list-series`, or a device connected after the fact. For what the\ningest path is doing while it runs, use `liveone queue`.\n\nAbout `liveone` generally: The entry point for operating LiveOne from a terminal or an agent. Pick the domain that owns\nthe thing you want to read or change — `dashboard` for what a dashboard SHOWS — then the verb.\nRun `liveone <domain> --help` to see a domain's verbs.\n\nAdmin/owner only, http-only. Prints `target: <origin> as <you>` on stderr first.\n\nReports PUBLISHED and LANDED as separate numbers, and never says \"inserted\". Publishing is\nthe near end of an asynchronous pipeline: on 2026-09-09 a backfill reported success ten times\nwhile materialising zero rows. `landed` is a read of the serving store AFTER the lane drains,\nthrough the same path a dashboard would use.\n\nChunked to the VENDOR's own window (Amber answers at most 7 days), so the caller passes the\nrange it wants and never a number the vendor imposed. Every message rides the `backfill` lane.\n\nCovers every vendor with a history API: amber, sigenergy, openelectricity. A live-poll vendor\n(selectronic, mondo, tesla) has no history endpoint at all, and is refused rather than\nno-op'd. A sync PUBLISHES; it does not rebuild derived tables — run `liveone device recompute`\nfor the same window afterwards, which a run that published anything reminds you to do.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone sync 10002 --start=2026-07-07 --end=2026-09-08 --action=usage\n  liveone sync 10002 --start=2026-07-07 --end=2026-09-08 --action=usage --apply",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "A device: its dv_… id, integer handle, slug, or name"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "start": {
            "type": "string",
            "description": "First local day to re-fetch (inclusive) (YYYY-MM-DD)"
          },
          "end": {
            "type": "string",
            "description": "Last local day to re-fetch (inclusive) (YYYY-MM-DD)"
          },
          "action": {
            "type": "string",
            "description": "AMBER ONLY — which half to fetch: usage (energy + cost), pricing (rates), or both (default: both). Prefer the narrowest that covers the gap. Refused for a vendor with one historical surface. (action)",
            "enum": [
              "usage",
              "pricing",
              "both"
            ]
          },
          "verify": {
            "type": "boolean",
            "description": "After publishing, wait for the lane to drain and read the serving store back. --no-verify skips it, and the report then says the landing was NOT checked.",
            "default": true
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        },
        "required": [
          "device"
        ]
      },
      "signature": "liveone sync <device> { --base-url?: string; --start?: string; --end?: string; --action?: \"usage\"|\"pricing\"|\"both\"; --verify?: boolean }",
      "invocation": "npm run liveone -- sync",
      "file": "scripts/ops/liveone.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__user__find",
      "summary": "Search CLERK for a user — including one who owns nothing.",
      "when": "Use this when `list` does not show them. `list` is derived from device ownership, so a\nnewly invited user is invisible to it by definition — and that is the user a transfer is\nusually about to hand something to.",
      "parentWhen": "Reach for this to see the platform's users and their device ownership. ADMIN-ONLY: a\nnon-admin CLI token is refused server-side.",
      "description": "npm run liveone -- user find — Search CLERK for a user — including one who owns nothing.\n\nUse this when `list` does not show them. `list` is derived from device ownership, so a\nnewly invited user is invisible to it by definition — and that is the user a transfer is\nusually about to hand something to.\n\nAbout `user` generally: Reach for this to see the platform's users and their device ownership. ADMIN-ONLY: a\nnon-admin CLI token is refused server-side.\n\nAn exact email is matched as an email; anything else is a fuzzy search over name,\nusername and email. Exit 1 when nothing matches, so it composes into a check.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = no user matched.\n\nExamples:\n  liveone user find karoline@example.com\n  liveone user find karoline",
      "input_schema": {
        "type": "object",
        "properties": {
          "search": {
            "type": "string",
            "description": "An email, or part of a name/username"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "search"
        ]
      },
      "signature": "liveone user find <search> { --base-url?: string }",
      "invocation": "npm run liveone -- user find",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__user__list",
      "summary": "List users: Clerk id, email, devices owned.",
      "when": "Start here when you do not know a user's id.",
      "parentWhen": "Reach for this to see the platform's users and their device ownership. ADMIN-ONLY: a\nnon-admin CLI token is refused server-side.",
      "description": "npm run liveone -- user list — List users: Clerk id, email, devices owned.\n\nStart here when you do not know a user's id.\n\nAbout `user` generally: Reach for this to see the platform's users and their device ownership. ADMIN-ONLY: a\nnon-admin CLI token is refused server-side.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone user list",
      "input_schema": {
        "type": "object",
        "properties": {
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "liveone user list { --base-url?: string }",
      "invocation": "npm run liveone -- user list",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "liveone__user__show",
      "summary": "One user's directory entry, with their owned devices.",
      "when": "Use this for one user's detail — devices, admin flag, default dashboard.",
      "parentWhen": "Reach for this to see the platform's users and their device ownership. ADMIN-ONLY: a\nnon-admin CLI token is refused server-side.",
      "description": "npm run liveone -- user show — One user's directory entry, with their owned devices.\n\nUse this for one user's detail — devices, admin flag, default dashboard.\n\nAbout `user` generally: Reach for this to see the platform's users and their device ownership. ADMIN-ONLY: a\nnon-admin CLI token is refused server-side.\n\nRead-only. This command changes nothing.\n\nCalls the deployed LiveOne API as the signed-in user. A missing, expired or revoked CLI token is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  liveone user show simon@example.com\n  liveone user show user_2yjTPLLmU2vMs4Vy4Q7g0Yy0abc",
      "input_schema": {
        "type": "object",
        "properties": {
          "user": {
            "type": "string",
            "description": "A user: their user_… Clerk id, email, or username"
          },
          "base-url": {
            "type": "string",
            "description": "Target origin (default: your stored default, else https://www.liveone.energy) (origin)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "user"
        ]
      },
      "signature": "liveone user show <user> { --base-url?: string }",
      "invocation": "npm run liveone -- user show",
      "file": "scripts/ops/liveone.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "pg-migrate",
      "summary": "Apply pending Postgres migrations to a PlanetScale branch, leaving nothing owned by a temp role.",
      "when": "Use this for EVERY migration applied to prod `sydney`, in place of `npm run db:pg:migrate`.\nProd has no stored connection string, so applying there means minting a role — and Postgres\nmakes that role the OWNER of what it creates, which the app (connecting as `postgres`) then\ncannot read. Also reach for it with --audit when a `pscale role delete` refuses, or when a\ntable that exists returns 'permission denied'.\nFor liveone-dev use plain `npm run db:pg:migrate`: .env.local already points at the\npersistent `postgres` role, so nothing is minted and the trap cannot arise.",
      "parentWhen": "",
      "description": "npm run pg-migrate -- — Apply pending Postgres migrations to a PlanetScale branch, leaving nothing owned by a temp role.\n\nUse this for EVERY migration applied to prod `sydney`, in place of `npm run db:pg:migrate`.\nProd has no stored connection string, so applying there means minting a role — and Postgres\nmakes that role the OWNER of what it creates, which the app (connecting as `postgres`) then\ncannot read. Also reach for it with --audit when a `pscale role delete` refuses, or when a\ntable that exists returns 'permission denied'.\nFor liveone-dev use plain `npm run db:pg:migrate`: .env.local already points at the\npersistent `postgres` role, so nothing is minted and the trap cannot arise.\n\nReports the target (with the branch id read from the CONNECTION USERNAME, the only place it\nis legible), the pending migrations, and any object in `public` not owned by `postgres`.\nWith --apply it applies, reassigns ownership to `postgres`, and re-sweeps to prove it.\nThe reassign+delete runs in a `finally`, so an interrupted or failed run still cleans up.\n🛑 It does NOT check that the migration was correct — gates belong in the migration file,\ninside the migrator's transaction. This guarantees ownership, and nothing else.\n\nWRITES. Dry by default: without --apply nothing changes and the report says what would. Off a terminal --apply additionally requires --yes, or it refuses with exit 2.\n\nConnects DIRECTLY to Postgres using the connection string in the environment. The printed `target:` line names the database, role and host — read it before writing.\n\nExit codes: 0, 1, 2, 5, 130. For this command, 1 = ownership debt found, or migrations are pending in a dry run.\n\nExamples:\n  npm run db:pg:migrate:prod\n  npm run db:pg:migrate:prod -- --apply\n  npm run db:pg:migrate:prod -- --audit\n  npm run db:pg:migrate:prod -- --branch restore-drill --audit",
      "input_schema": {
        "type": "object",
        "properties": {
          "database": {
            "type": "string",
            "description": "PlanetScale database (name)",
            "default": "liveone"
          },
          "branch": {
            "type": "string",
            "description": "Branch to apply to (prod is `sydney`) (name)",
            "default": "sydney"
          },
          "role-ttl": {
            "type": "string",
            "description": "TTL for the minted role (duration)",
            "default": "1h"
          },
          "audit": {
            "type": "boolean",
            "description": "Ownership sweep only — mints a READ-ONLY role and applies nothing"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          },
          "apply": {
            "type": "boolean",
            "description": "Actually write. Without it nothing changes and the report says what would."
          },
          "yes": {
            "type": "boolean",
            "description": "Confirm without prompting. Required alongside --apply when stdin is not a terminal."
          }
        }
      },
      "signature": "pg-migrate { --database?: string; --branch?: string; --role-ttl?: string; --audit?: boolean }",
      "invocation": "npm run pg-migrate --",
      "file": "scripts/ops/pg-migrate.ts",
      "writes": true,
      "exitCodes": [
        0,
        1,
        2,
        5,
        130
      ]
    },
    {
      "name": "selectlive__auth",
      "summary": "Verify and save the portal login, inspect authentication, or forget credentials.",
      "when": "Verify and save the portal login, inspect authentication, or forget credentials.",
      "parentWhen": "Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.",
      "description": "npm run selectlive -- auth — Verify and save the portal login, inspect authentication, or forget credentials.\n\nVerify and save the portal login, inspect authentication, or forget credentials.\n\nAbout `selectlive` generally: Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.\n\nNo action means login. Password input is hidden. Automation: SELECTLIVE_EMAIL and SELECTLIVE_PASSWORD.\n--device additionally verifies and saves the inverter password (or SELECTLIVE_INVERTER_PASSWORD).\nstatus verifies the saved portal account. logout removes the local store without contacting Select.live.\nStore: $XDG_CONFIG_HOME/selectlive/credentials.json, or ~/.config/selectlive/credentials.json (mode 600).\n\nlogin/logout change the local credential file; status only verifies authentication.\n\nUses local credentials and verified TLS to select.live:7528. Portal or inverter authentication failure is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  selectlive auth\n  selectlive auth --device 123456\n  selectlive auth status\n  selectlive auth logout",
      "input_schema": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "description": "login (default), status, or logout"
          },
          "device": {
            "type": "string",
            "description": "Inverter serial (required when the account has multiple devices)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "selectlive auth [action] { --device?: string }",
      "invocation": "npm run selectlive -- auth",
      "file": "scripts/selectlive.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "selectlive__devices",
      "summary": "List inverter serials accessible to the portal account.",
      "when": "List inverter serials accessible to the portal account.",
      "parentWhen": "Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.",
      "description": "npm run selectlive -- devices — List inverter serials accessible to the portal account.\n\nList inverter serials accessible to the portal account.\n\nAbout `selectlive` generally: Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.\n\nThe legacy device-list protocol supplies serial numbers only; it does not report online status.\n\nRead-only. This command changes nothing.\n\nUses local credentials and verified TLS to select.live:7528. Portal or inverter authentication failure is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  selectlive devices",
      "input_schema": {
        "type": "object",
        "properties": {
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "selectlive devices",
      "invocation": "npm run selectlive -- devices",
      "file": "scripts/selectlive.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "selectlive__history__download",
      "summary": "Preserve the full detailed buffer and optionally decode it as CSV.",
      "when": "Preserve the full detailed buffer and optionally decode it as CSV.",
      "parentWhen": "Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.",
      "description": "npm run selectlive -- history download — Preserve the full detailed buffer and optionally decode it as CSV.\n\nPreserve the full detailed buffer and optionally decode it as CSV.\n\nAbout `selectlive` generally: Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.\n\nAlways acquires the complete retained buffer; start/end filter only the exported CSV.\nRaw records and manifest are preserved before decoding. CSV requires --timezone.\nUnknown formats, moving buffers, and conversion failures remain explicit in the manifest.\nTimestamps use the device's local clock (epoch 2001-01-01). DST ambiguity is rejected.\nEach invocation creates a fresh directory; existing downloads are never overwritten.\n\nCreates a new acquisition directory containing raw records, a manifest, and optional CSV.\n\nUses local credentials and verified TLS to select.live:7528. Portal or inverter authentication failure is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130. For this command, 1 = raw download preserved, but incomplete, unsupported, or not decoded.\n\nExamples:\n  selectlive history download --device 123456 --out ./downloads --timezone Australia/Melbourne --start 2026-09-10 --end 2026-09-11\n  selectlive history download --device 123456 --out ./downloads",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "Inverter serial (required when the account has multiple devices)"
          },
          "out": {
            "type": "string",
            "description": "Parent directory for the new acquisition"
          },
          "timezone": {
            "type": "string",
            "description": "Device clock's IANA timezone; required to produce CSV"
          },
          "start": {
            "type": "string",
            "description": "Inclusive local start date (YYYY-MM-DD)"
          },
          "end": {
            "type": "string",
            "description": "Exclusive local end date (YYYY-MM-DD)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "out"
        ]
      },
      "signature": "selectlive history download { --device?: string; --out: string; --timezone?: string; --start?: string; --end?: string }",
      "invocation": "npm run selectlive -- history download",
      "file": "scripts/selectlive.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "selectlive__history__info",
      "summary": "Read detailed log metadata and the oldest/newest record timestamps.",
      "when": "Read detailed log metadata and the oldest/newest record timestamps.",
      "parentWhen": "Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.",
      "description": "npm run selectlive -- history info — Read detailed log metadata and the oldest/newest record timestamps.\n\nRead detailed log metadata and the oldest/newest record timestamps.\n\nAbout `selectlive` generally: Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.\n\nRead-only. This command changes nothing.\n\nUses local credentials and verified TLS to select.live:7528. Portal or inverter authentication failure is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  selectlive history info --device 123456",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "Inverter serial (required when the account has multiple devices)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "selectlive history info { --device?: string }",
      "invocation": "npm run selectlive -- history info",
      "file": "scripts/selectlive.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "selectlive__info",
      "summary": "Read inverter identity, firmware, interface versions, and logging metadata.",
      "when": "Read inverter identity, firmware, interface versions, and logging metadata.",
      "parentWhen": "Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.",
      "description": "npm run selectlive -- info — Read inverter identity, firmware, interface versions, and logging metadata.\n\nRead inverter identity, firmware, interface versions, and logging metadata.\n\nAbout `selectlive` generally: Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.\n\nRead-only. This command changes nothing.\n\nUses local credentials and verified TLS to select.live:7528. Portal or inverter authentication failure is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  selectlive info --device 123456",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "Inverter serial (required when the account has multiple devices)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        }
      },
      "signature": "selectlive info { --device?: string }",
      "invocation": "npm run selectlive -- info",
      "file": "scripts/selectlive.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    },
    {
      "name": "selectlive__read",
      "summary": "Read a bounded range of inverter memory without writing settings.",
      "when": "Read a bounded range of inverter memory without writing settings.",
      "parentWhen": "Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.",
      "description": "npm run selectlive -- read — Read a bounded range of inverter memory without writing settings.\n\nRead a bounded range of inverter memory without writing settings.\n\nAbout `selectlive` generally: Authenticate, list inverters, inspect memory, or preserve retained detailed history through the SP LINK tunnel.\n\nRead-only. This command changes nothing.\n\nUses local credentials and verified TLS to select.live:7528. Portal or inverter authentication failure is exit 3.\n\nExit codes: 0, 1, 2, 3, 5, 130.\n\nExamples:\n  selectlive read --device 123456 --address 0xa007 --words 6",
      "input_schema": {
        "type": "object",
        "properties": {
          "device": {
            "type": "string",
            "description": "Inverter serial (required when the account has multiple devices)"
          },
          "address": {
            "type": "string",
            "description": "Word address, in decimal or 0x hexadecimal"
          },
          "words": {
            "type": "number",
            "description": "Number of 16-bit words (1–256)"
          },
          "format": {
            "type": "string",
            "enum": [
              "human",
              "json"
            ],
            "description": "Output format (default: json off a terminal)"
          }
        },
        "required": [
          "address",
          "words"
        ]
      },
      "signature": "selectlive read { --device?: string; --address: string; --words: number }",
      "invocation": "npm run selectlive -- read",
      "file": "scripts/selectlive.ts",
      "writes": false,
      "exitCodes": [
        0,
        1,
        2,
        3,
        5,
        130
      ]
    }
  ]
}
